# Changelog

## 2026-07-18 — 직접 수정 관찰 확장 (서식·말투)

- JARVIS 검증 편집 직후의 직접 수정을 한 사건당 하나의 관찰로 분류한다.
  우선순위는 축약 > 말투 > 서식이며 모호한 사건은 해석하지 않는다.
- 문장 어미 기반 결정적 말투 분류(합쇼체·해요체·해라체)를 추가하고, 비슷한
  길이의 재작성에서 명확한 어미 전환만 `격식체`·`친근한 문체` 증거로 기록한다.
  저장 값은 어미 분류 한 단어이며 문장 원문은 남기지 않는다.
- 텍스트 지문이 같고 굵기·글자 크기(1pt 이상)·문단 정렬 중 정확히 하나만 바뀐
  Word·PowerPoint 직접 수정을 `텍스트 강조 방식`·`글자 크기 방향`·`문단 정렬
  기본값` 증거로 기록한다. 혼합·미정 native sentinel과 다중 변경은 제외한다.
- 증거 3회 뒤 안내되는 `앞으로도 …해줘` 확인 문장이 현재 파일에 대기 중인
  같은 값의 후보를 실제로 활성화하도록 연결했다. 이전에는 안내 문구가 무관한
  전역·업무 후보를 새로 만드는 공백이 있었다.
- `앞으로`·`항상` 같은 일반화 표현이 있을 때만 서식 선호 명시 문장을 해석하고,
  `가운데 정렬로 해줘` 같은 일반 명령은 문서 편집으로 남긴다.
- 커서로 접힌 선택의 앱별 증명을 연구해 Goal 문서에 기록했다. 세 앱 모두 현재
  개인정보 경계 안에서는 구현하지 않으며 PowerPoint shape 전체 텍스트 지문
  방식을 후속 후보로 남겼다.
- 전체 자동 회귀 755개 중 752개 통과(unit 259·integration 295·windows 198),
  외부 AI 라이브 3개 skip, 실패 0을 확인했다.

## 2026-07-18 — 실행 전 공통 복구 계약·연결 문서 focus 복구·교정 학습 확장

- 실행 전임을 증명할 수 있고 대상 서명이 같을 때만 최대 1회 허용하는 공통 복구
  계약(`engine/recovery`)을 추가했다. 복구 증거에는 해시·불리언·횟수만 남기고
  앱 이름·경로·사용자 원문은 저장하지 않는다.
- 등록되지 않은 앱 열기·닫기 대상은 실행 전에 Windows App Paths와 시작 메뉴·
  바탕화면 바로가기를 한 번만 재탐색하고, 미발견 시 반복 탐색 없이 사용자에게
  묻는다.
- 명시적 편집 요청의 준비·승인 직전에 연결 문서가 다른 창 뒤로 밀린 경우 그
  문서 창만 한 번 활성화하고 동일 문서 fingerprint를 재확인한 뒤에만 미리보기와
  승인 실행을 계속한다. 평소 상태 조회는 창 focus를 바꾸지 않는다.
- 실패 결과를 결정적 책임 분류(runtime_recoverable, needs_input,
  missing_capability, implementation_bug, environment_blocked, policy_blocked,
  preference_mismatch)로 나누는 failure triage와 익명 개발 이슈 기록을 추가했다.
- 미리보기 재작성·취소 사유와 검증된 후속 교정, JARVIS 편집 직후 같은 구조적
  시작점의 명확한 직접 축약을 파일 범위 선호 학습 증거로 구조화했다. 같은 값
  3회와 사용자 승인이 있어야 기본값으로 활성화된다.
- 편집 대상 표시 bar와 선택 overlay UI를 추가해 현재 편집 대상과 선택 영역을
  화면에서 확인할 수 있게 했다.
- 앱 실행 차단 정책을 `engine/security/launch_policy`로, 시스템 볼륨 제어를
  `engine/system_volume`으로 분리했다.
- 전체 자동 회귀 729개 중 726개 통과, 외부 AI 라이브 3개 skip, 실패 0을
  확인했다.

## 1.1.0-rc.6 — 독립 감사 후속 안전성 보완

- 편집 문맥의 1초 COM polling을 제거하고 연결·탭 진입·focus·수동 새로고침·
  명령 전·처리 후 이벤트에서만 갱신하도록 바꿨다.
- 편집 전 문맥 갱신을 await하고 요청 `context_fingerprint`와 prepare 시점 문맥이
  다르면 미리보기를 만들기 전에 차단한다.
- 새 문서 workflow는 승인 전 영구 JSON을 만들지 않고 승인 순간 원본·출력 충돌을
  다시 검사한 뒤 최초 저장한다. 취소·승인 대기 상태는 재개 대상에서 제외했다.
- PPT 출력명에 실제 장수를 반영하고 Word 보고서에는 원본 basename만 넣는다.
- 11단계 범위를 명시적 사용자 선호 학습 v1로 정확히 한정했다.
- VBA 프로시저 호출 반환과 셀·파일 등 업무 결과 검증을 별도 상태로 구분했다.
- clean commit 전용 `git archive` 소스 공유 스크립트와 export 제외 규칙을 추가했다.
- 개인 PC 런타임 절대 경로를 문서에서 제거하고 과거 감사의 대상 버전을 명시했다.
- 전체 자동 회귀 632개 중 629개 통과, 외부 AI 라이브 3개 skip, 실패 0개를 확인했다.
- 8단계 네 앱 각 100/100, 9단계 실제 VBA 백업·수정·복원·실행, 10·11·12단계
  전용 프로브를 모두 다시 통과했다.
- 이 Office 빌드에서 VBA 프로젝트 접근 뒤 Excel `Quit()`가 지연되는 경우를 위해,
  검증 시작 전 존재하지 않았고 검증 중 생성된 Excel PID만 종료하는 소유 인스턴스
  정리 폴백을 추가했다.
- Python 매크로 timeout은 실제 자식 프로세스 생성 뒤부터 계산하고, 임시파일 준비
  중 취소가 도착하면 프로세스를 새로 만들기 직전에 다시 차단하도록 보강했다.
- 실제 Eel 브리지를 띄운 GUI에서 편집 모드 전환, 문맥 수동 새로고침과 연결 전
  안내 상태를 확인했고 현재 페이지 콘솔 오류 0건을 기록했다.

## 2026-07-17 — Prototype 1.0 8단계·Prototype 1.1 9단계 안정화 재검

- PowerPoint 선택 객체를 `SlideID`·`ShapeID`로 다시 획득하고 이동·크기 변경의
  쓰기, 재읽기, 복원 결과를 새 COM 프록시로 검증하도록 보강했다.
- 끊어진 PowerPoint COM 프록시는 쓰기 전까지만 제한 재시도하고, 쓰기 이후에는
  중복 편집을 막기 위해 재시도하지 않는다.
- PowerPoint 보완 후 100회 실검을 다섯 회 수행해 누적 500/500, 각 회 COM
  interface 증가 0, 적용·복원·프로세스 정리를 확인했다.
- 한글 실검을 JARVIS 소유 전용 미저장 문서에서 수행하고 worker 진행 단계와
  timeout 진단을 남기도록 바꿨다. 네 앱 전체 100회 실검이 통과했다.
- Excel VBA 신뢰 설정을 읽기 전용으로 진단하고 `VbaTrustAccessBlocked`로
  일반 오류와 명확히 구분하며 실패 단계·재시도 여부·진단 문맥을 API까지 전달한다.
- VBA 정적 분석을 실제 프로젝트 접근보다 먼저 실행하고, 쓰기와 복원이 모두
  실패해도 외부 `.bas` 백업을 보존하도록 검증했다.
- 전체 자동 회귀 623개 중 620개 통과, 외부 AI 3개 skip, 실패 0개를 확인했다.
- 실제 VBA 프로젝트 접근은 이 PC의 Excel 신뢰 설정으로 차단됐고, 설정을
  변경하거나 우회하지 않았다.

## Prototype 1.1 12단계 - 자가 진단·안전한 개선 제안

- 실패 실행을 개인정보가 제거된 사건으로 수집하고 같은 구조 실패를 fingerprint로
  그룹화하는 `DiagnosticIncidentManager`를 추가했다.
- 실패 단계·기대/실제 상태·앱 버전·원인 후보·합성 재현 규격·필수 회귀 테스트를
  한 보고서로 조회하도록 했다.
- 성공·취소·확인 대기·busy와 진단 자체의 빈 결과는 사건에서 제외했다.
- 사용자 명령·경로·문서 내용·오류 문장을 저장하지 않고 구조 식별자·확장자·
  해시만 보존하도록 했으며 기존 실행 진단과 백업도 익명 형식으로 마이그레이션한다.
- “최근 오류 진단해줘” 자연어 명령과 사건 목록·보고서·상태·건강 요약·수정 제안
  API를 추가했다.
- 수정 제안은 사용자 승인, 분리 worktree, 테스트 우선, 전체 회귀, 별도 빌드 승인,
  기존 실행본 롤백 경계를 명시하며 어떤 단계도 자동 실행하지 않는다.
- 합성 실패 주입에서 사용자 문서·Office 앱·worktree·빌드 없이 개인정보 부재와
  실행 바이너리 불변을 검증했다.
- 12단계 추가 후 전체 자동 회귀 614개 중 611개 통과·외부 AI 3개 skip·실패
  0개를 확인했다.

## Prototype 1.1 11단계 - 명시적 사용자 선호 학습 v1

- 사용자 업무 선호를 기존 실행 방식 선호와 분리해 저장하는
  `UserPreferenceLearningManager`를 추가했다.
- 1회 관찰, 3회 반복 후보 승격, 사용자 승인 후 활성화라는 보수적 학습 정책과
  중복 증거·상충 값·후보 취소 처리를 구현했다.
- 전역·앱·업무·파일 범위와 `파일 > 업무 > 앱 > 전역` 우선순위를 추가했다.
- 요약 길이, 문체, 제목, 숫자, 표, PPT 장수, 파일 위치, 확인 작업, 작업 순서,
  VBA 수정 패턴을 허용 목록과 값 범위로 제한했다.
- 승인된 선호를 10단계 Word/PPT 생성 기본값에 적용하고, 현재 명령의 명시적
  값이 학습값보다 우선하도록 했다.
- Word 세션 학습, 후보 승인·취소, 7장 PPT 기본값, 명시적 5장 우선, VBA 패턴
  보완을 단위·통합 테스트로 검증했다.
- 소유 임시 Office 문서에서 승인 선호 재시작 유지와 실제 7장 PPT 생성을
  재열기 검증했으며 사용자 문서·선호 저장소를 변경하지 않았다.
- 11단계 추가 후 전체 자동 회귀 601개 중 598개 통과·외부 AI 3개 skip·실패
  0개를 확인했다.

## Prototype 1.1 10단계 - 앱 간 문서 워크플로

- 연결 Excel을 읽기 전용으로 분석해 공통 JSON 업무 데이터, Word 보고서,
  PowerPoint 5장 요약으로 잇는 `WorkflowExecutor`를 추가했다.
- 승인 전에는 산출물을 만들지 않고, 기존 파일은 고유 이름으로 피하며,
  Excel 원본 지문이 바뀌면 실행을 차단한다.
- 현재 단계·성공 단계·실패 단계·생성 파일·파일 지문·검증 결과를 원자적으로
  저장하고 실패 단계만 재시도한다.
- Word 생성 후 PPT 실패를 주입해 재개 시 Excel 분석과 Word가 중복 실행되지
  않는 단위·통합 테스트를 추가했다.
- JARVIS 소유 임시 Excel·Word·PowerPoint로 보고서 재열기, PPT 정확히 5장,
  원본 불변, 프로세스 정리를 실제 검증했다.
- 10단계 추가 후 전체 자동 회귀 581개 중 578개 통과·외부 AI 3개 skip·실패
  0개를 확인했다.

## 개발 중 — Prototype 1.0·1.1 편집모드

- `v1.1.0-rc.5`에서 `feature/prototype-1.0-edit-mode` 브랜치를 분리했다.
- Excel·한글·Word·PowerPoint의 COM 등록, 앱 버전, 선택 읽기, 임시 편집,
  Undo, 저장 재개봉, 소유 프로세스 정리를 확인하는 1단계 검증 도구를 추가했다.
- 기존 사용자 앱이 실행 중이면 실제 앱 검증을 거부하고, JARVIS가 생성한 빈
  임시 문서만 사용하는 보호 경계를 추가했다.
- COM 변경이 앱의 네이티브 Undo 이력에 항상 등록되지 않는 점을 반영해,
  네이티브 Undo와 변경 전 스냅샷 복원을 구분해 검증하도록 했다.
- 편집 요청·준비 작업·실행 결과를 COM-free JSON 계약으로 정의하고, 앱
  어댑터 허용 작업만 준비·실행·검증·복구하는 공통 coordinator를 추가했다.
- 편집 세션의 연결·준비·승인·실행·검증·커밋·복구 상태와 revision 충돌
  검사를 추가했다.
- `edit` 요청을 기존 명령 파이프라인과 분리하고, 세션 정보가 없거나 아직
  핸들러가 연결되지 않았으면 실행하지 않도록 했다.
- 알 수 없는 모드의 명령 폴백과 질문·대화모드의 앱 실행을 백엔드에서
  차단하는 모드 권한 정책을 추가했다.
- GUI에 `명령 | 질문 | 편집` 탭과 파일 선택·드롭, 열린 문서 연결, 연결 해제,
  자동 창 배치 컨트롤을 추가했다.
- Excel·한글·Word·PowerPoint 지원 확장자와 앱 설치 여부를 확인하고, 열린
  네이티브 문서의 전체 경로가 일치할 때만 세션을 만드는 `FileIntakeManager`를
  추가했다.
- 세션에는 COM 객체 대신 파일 경로·앱 유형·파일 식별 fingerprint·선택 참조만
  저장하고, 파일 교체나 다른 세션 요청을 `stale_context`로 차단한다.
- 파일 경로 ROT moniker fallback을 추가해 Office Application이 ROT에 늦게
  등록되는 경우에도 선택한 Workbook·Document·Presentation을 정확히 찾는다.
- 문서 모니터 기준 68:32 자동 배치를 세션당 한 번만 시도하고, 배치 실패가
  문서 연결을 취소하지 않도록 격리했다.
- 실제 임시 Excel·Word·PowerPoint 파일의 연결·세션·프로세스 정리를 검증했고,
  실행 중인 사용자 한글 문서는 읽기 전용 세션 탐색만 수행해 보존했다.
- Excel·한글·Word·PowerPoint의 현재 컨테이너, 셀/문자 Range, 커서, 슬라이드,
  Shape를 읽기 전용으로 캡처하는 앱별 `ContextProvider`와 `EditContextManager`를
  추가했다.
- 파일 식별 `document_fingerprint`와 선택 상태 `context_fingerprint`를 분리하고,
  같은 선택은 안정적으로 유지하면서 선택 위치나 선택 내용이 바뀌면 문맥
  fingerprint가 달라지도록 했다.
- 연결 문서가 닫히거나 다른 문서가 활성화되거나 파일이 교체되면 문맥 조회와
  편집 요청을 `stale_context`로 차단한다.
- 편집 화면에 현재 시트·범위, 문장 Range, 슬라이드·Shape와 제한된 텍스트
  미리보기를 표시하고, 편집 탭이 보이는 동안 문맥을 자동 갱신한다.
- Excel·한글의 제한된 한국어 편집 의도를 기존 네이티브 PreparedAction으로
  변환하는 5단계 어댑터와 모든 쓰기의 변경 미리보기·승인 흐름을 추가했다.
- Excel 값·수식·합계·평균·서식·필터·정렬·찾기/바꾸기·행/열 삽입과 한글
  선택 교체·로컬 축약/격식체·글자/문단 서식·찾기/바꾸기를 지원한다.
- 승인 직전 4단계 문맥과 네이티브 fingerprint를 함께 재검증하고, 한글 선택이
  사라진 경우 커서 입력으로 바뀌지 않도록 전체 선택 digest와 길이를 확인한다.
- 취소·실패·오래된 승인 뒤 편집 세션을 `ready`로 정리하고, 실행 성공은
  네이티브 재읽기 검증이 통과한 경우에만 반환한다.
- Word 선택 Range 텍스트 교체, 굵기·글자 크기, 문단 정렬, Style·
  표 셀 조회와 저장을 승인·재읽기 검증 흐름으로 추가했다.
- PowerPoint 단일 Shape/텍스트 교체·서식·정렬·이동·크기 변경과
  Placeholder 식별, 앞 슬라이드 동일 역할 Shape 스타일 복사를 추가했다.
- Word는 문자 Range·전체 문서 digest·표 셀을, PowerPoint는 슬라이드 ID·
  Shape ID·선택 텍스트 digest를 확인 전·후 비교하며 불일치 시 차단한다.
- 직전 편집 후의 정확한 문맥에서만 `조금 더`, `너무 길어`, `다시`,
  `두 번째 문장만`, `마지막 문장은 원래대로`, `아까처럼` 같은 후속 명령을
  확장하고 같은 대상의 연속 편집 횟수를 세션에 기록한다.
- 텍스트 미리보기에 안전한 대안을 만들 수 있을 때 `다시 작성` 선택지를
  추가하고, 기존 준비 작업을 실행하지 않은 채 새 request ID의 미리보기로
  교체한다.
- Excel·한글·Word·PowerPoint의 직전 JARVIS 편집 한 건을 구조화된 원본
  스냅샷과 네이티브 Undo 보조 수단으로 복원한 뒤 다시 읽어 검증한다.
- 대상 변경이나 문서 종료 시 연속 수정·복원 기록을 폐기하고, 복원 성공
  뒤에도 기록을 비워 중복 복원을 차단한다.
- 한 controller에서 Excel→한글→Word→PowerPoint 대표 자연어 편집을
  순차 실행하고, 앱 전환 뒤 이전 세션 요청과 닫힌 문서의 후속 명령을
  차단하는 8단계 통합 회귀를 추가했다.
- 네 앱 각각 100회 연속 네이티브 편집, 읽기 전용·보호 문서, 같은 이름의
  다른 파일, 예외 뒤 세션 복구와 재시작 초기화를 검사한다.
- PowerPoint의 일시적인 COM busy·호출 거부·비활성 프레젠테이션 응답에만
  제한 재시도를 적용하고, Shape 선택 조회 오류가 다른 대상으로 우회되지
  않게 했다.
- JARVIS 소유 네 앱 임시 문서에서 앱별 100/100 편집, COM interface 증가 0,
  대표 자연어 적용·검증·복원과 소유 프로세스 정리를 실제 검증해
  `prototype_1_0_ready=true`를 확인했다.
- 8단계 전체 자동 회귀 555개 중 552개 통과·외부 AI 3개 skip·실패 0개를
  확인했다.
- `.xlsm/.xlsb` VBA 프로젝트·모듈·Sub·Function 목록, 단일 모듈 코드 읽기와
  로컬 구조·오류·위험 분석을 제공하는 9단계 Excel VBA 어댑터를 추가했다.
- 마지막 행 계산 보정, UsedRange의 Selection 제한, 원본 시트 백업 삽입,
  정확히 한 곳의 코드 조각 교체를 unified diff 미리보기로 준비한다.
- 표준 모듈 수정 전에 `%LOCALAPPDATA%\Jarvis\data\vba_backups`에 원본 `.bas`를
  내보내고, 변경 후 digest가 다르거나 예외가 발생하면 메모리 원문으로 복원한다.
- 매크로 실행을 코드 수정과 분리된 고위험 승인 작업으로 만들고, Shell·파일
  삭제·네트워크·레지스트리 기능이 감지되면 강한 경고와 두 번째 승인을 요구한다.
- 9단계 추가 후 전체 자동 회귀 572개 중 569개 통과·외부 AI 3개 skip·실패
  0개를 확인했다.
- JARVIS 소유 임시 `.xlsm` 실제 검증은 Excel 보안 센터의 VBA 프로젝트 개체
  모델 접근 설정에 의해 `blocked_vba_trust`로 안전하게 차단됐으며, 사용자
  문서 변경이나 좀비 Excel 프로세스는 없었다.
- JARVIS 소유 임시 Word 문서에서 선택 문장 5회 연속 축약, 선택 유지,
  연속 횟수 `1~5`, 직전 편집 복원과 세션 `ready` 복귀를 실제 검증했다.
- 7단계 전체 자동 회귀 540개 중 537개 통과·외부 AI 3개 skip·실패 0개를
  확인했다.
- 자동 회귀 525개 중 522개 통과·외부 AI 3개 skip·실패 0개를 확인했고,
  JARVIS 소유 네 앱 임시 문서에서 동일 선택 안정성과 선택 변경 감지를
  검증한 데 이어 Excel 값 입력·행 삽입과 한글 선택 문장 교체를 실제 승인
  흐름으로 검증했다. Word 문장 교체·글자 크기·저장과 PowerPoint
  제목 교체·Shape 이동·앞 슬라이드 스타일 복사도 실제 검증한 뒤 소유
  프로세스가 남지 않은 것을 확인했다.

## 1.1.0-rc.5 — 브라우저 폴백과 Office COM 생명주기

- Chrome, Edge, 시스템 기본 브라우저, 추가 설치 브라우저 순서의 GUI 실행
  폴백과 사용자 오류 안내, 선택·실패 로그를 추가했다.
- Excel/HWP COM 작업마다 `CoInitialize()`와 `CoUninitialize()`를 짝지어
  호출하고, 기존 인스턴스와 Jarvis 생성 인스턴스의 소유권을 분리했다.
- Jarvis 소유 문서와 Application만 정리하며, 예외가 발생해도 소유
  Application 종료를 시도하도록 공통 생명주기 객체를 추가했다.
- 동적 Office 코드에도 COM 초기화·해제와 사용자 인스턴스 보존 규칙을
  적용했다.

## 1.1.0-rc.4 — 빈 앱 사전과 클라우드 AI 단순화

- 최초 앱 사전을 완전히 비우고 사용자가 앱·최근 앱·북마크 스캔을 실행한 뒤에만
  항목이 등록되도록 기본 앱·웹사이트 자동 주입을 제거했다.
- Ollama 제공자, 로컬 모델 설정, 상단 전환 토글과 클라우드 실패 시 자동 대체 경로를
  제거했다. AI 기능은 OpenAI 또는 Gemini API 키가 있을 때만 동작한다.

## 1.1.0-rc.3 — 단일 실행형 재부팅 보정

- PyInstaller one-file 실행본이 재부팅할 때 이전 `_MEI` 임시 경로를 재사용하지 않도록
  새 압축 해제 환경을 요청한다.
- 친구 전달용 단일 EXE에는 빈 기본 기억·빈 설정만 포함하고 대화 세션을 포함하지 않는다.

## 1.1.0-rc.2 — 보정 릴리스 후보

### 보안과 결과 정확성

- `PyPDF2`를 `pypdf 6.14.2`로 교체하고 PDF 크기·페이지·추출 문자 상한 추가
- 시스템 종료 명령을 `subprocess.run(..., shell=False)`로 전환하고 실제
  반환 코드에 따라 성공·실패 보고
- runtime 소스의 bare `except`, 비의도적 `print()` 경로 정리
- 사전·학습 GUI의 동적 HTML 삽입을 안전한 DOM 조립과 숫자 검증으로 전환

### 구조와 재현성

- 실행 폴더와 개발 저장소를 분리하고 테스트·검증·빌드 도구 복원
- 명령 파이프라인을 대화·네이티브·학습·매크로·AI fallback route로 분리
- AI batch 실행을 dispatcher, action executor, 결과 aggregator로 분리
- confirmation 생성과 학습 행동 재실행을 parser 밖의 서비스로 분리
- Excel·HWP의 COM-free helper 및 Word·PowerPoint 확장 계약 추가
- 동결 EXE 검증 probe를 현재 confirmation 정책과 실행 결과 구조에 맞게 갱신

### 최종 검증

- 자동 테스트 435개: 432개 통과, 외부 AI 3개 skip, 실패 0개
- `pip-audit` 알려진 취약점 0건
- 실제 Excel 20/20, 한글 8/8 통과
- 후보 EXE 기동, frozen worker, timeout·취소·재시도·잠금, TLS, Excel COM 통과
- 기존 사용자 데이터 33개 파일 사전·사후 바이트 동일
- 태그 `v1.1.0-rc.2`, 빌드 commit `ceb6c17d14f495d167a3c12417464c124bde0d90`

## 1.1.0 — 2026-07-15 이전 실행본

- HTML 정화와 로컬 마크다운 렌더러
- 동시 명령 busy 보호와 실행 진단
- 학습 네이티브 실행 경로 보강
- Excel·한글 네이티브 편집, 확인·검증·복구 기반

이 빌드는 R6 교체 전 current였으며, 현재는 `JARVIS_RUNTIME\previous`에
직전 정상 백업으로 보존돼 있습니다.

## 1.0 — 2026-07-14

- Windows 앱·파일·웹·미디어 기본 명령
- AI 질문·대화와 동적 행동 안전 검사
- 사용자 사전·매크로·세션 저장 기반
