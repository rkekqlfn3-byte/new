# Changelog

## 2026-07-18 — HWP Automation 보안 모듈 사전 검사

- 한컴 공식 Automation 보안 모듈이
  `HKCU\Software\HNC\HwpAutomation\Modules`에 등록돼 있고 등록 경로의 파일이
  실제로 존재하는지 읽기 전용으로 확인한다.
- 기본 한글 보고서 생성은 유효한 모듈이 없으면 HWP 프로세스를 시작하지 않고
  `environment_error`·재시도 가능으로 즉시 차단한다. JARVIS가 모듈 설치,
  레지스트리 등록 또는 보안 설정 변경을 대신하지 않는다.
- 유효한 등록이 있으면 모듈 값 이름을 그대로
  `RegisterModule("FilePathCheckDLL", 이름)`에 전달한다. 활성화 실패도 파일
  쓰기 전에 환경 차단한다.
- 실제 현재 PC에서는 0초대에 `security_module_unavailable_blocked`로
  복귀했고 부분 파일·생성 HWP 프로세스가 없었다. 별도 10단계 probe도
  `create_hwp_report`의 구조화된 `environment_error`와 재개 안내를 확인했다.

## 2026-07-18 — HWP 워크플로 저장 watchdog

- 기본 한글 보고서 생성 전체를 별도 소유 프로세스로 격리하고
  `JARVIS_HWP_WORKFLOW_TIMEOUT_SECONDS` 제한을 추가했다. 기본 60초이며 5~300초
  범위로 제한한다. 테스트용 주입 COM 경로는 기존처럼 프로세스 내부에서
  결정적으로 실행한다.
- HWP 저장 호출이 멈추면 작업자와 작업자가 보고한 단일 소유 HWP 프로세스만
  정리하고 부분 `.hwp` 파일을 제거한다. 기존 사용자 HWP PID는 종료하지 않는다.
- 시간 초과는 `timeout`·재시도 가능으로 보존해 `create_hwp_report` 실패 단계와
  이미 성공한 Excel 분석을 상태에 남긴다. 사용자는 환경 확인 뒤
  `실패한 워크플로 이어서`로 같은 단계부터 재개할 수 있다.
- 전용 실제 환경 probe는 이 PC의 HWP `SaveAs` 대기를 60.14초에
  `environment_timeout_handled`로 종료했고, 부분 파일 부재와 소유 프로세스
  정리를 확인했다. 정상 저장 환경에서는 생성·read-back 완료도 같은 probe의
  통과 결과다.

## 2026-07-18 — 최근 산출물 인계 후속 편집 실검

- 최근 검증 Word뿐 아니라 PowerPoint 산출물도 정확한 경로·앱·SHA-256
  지문으로 새 편집 세션에 인계되는 실제 Office 증거를 추가했다.
- 인계된 Word 제목 Range와 PowerPoint 제목 Shape를 각각 선택한 뒤
  `조금 크게 해줘`를 승인 실행했다. 두 앱 모두 글자 크기 +2pt read-back,
  동일 세션 유지와 `ready` 복귀를 실제 검증했다.
- 사용자 Office 프로세스가 있으면 probe를 실행하지 않고, JARVIS 소유 임시
  산출물만 편집한 뒤 생성 프로세스를 정리한다. 원본 Excel과 사용자 문서는
  수정하지 않으며 경로·내용을 검증 보고서에 남기지 않는다.
- 제품 Goal 게이트는 Word·PowerPoint 인계와 각 앱의 후속 선택 편집 증거를
  성공 작업 재사용 축의 필수 조건으로 요구한다.

## 2026-07-18 — 최근 검증 산출물 편집 세션 인계

- `방금 만든 보고서를 편집 문서로 연결해줘`처럼 `편집`과 `연결`을 모두
  명시한 경우에만 최근 검증 산출물을 새 편집 대상으로 전환한다. 단순
  `편집해줘`는 선택 대상이 불명확하므로 이 동작으로 추측하지 않는다.
- 기존 열기 계약과 동일하게 완료 워크플로, 정확한 경로, 앱과 SHA-256 지문을
  다시 검증한 뒤 파일을 연다. 검증된 파일을 `FileIntakeManager`로 다시
  발견한 경우에만 새 세션 ID와 문맥을 확정한다.
- 백엔드 인계가 성공하면 GUI가 새 세션과 문맥을 먼저 반영한 뒤 선택 상태를
  갱신한다. 다음 편집은 새 문서의 현재 선택·커서에만 적용된다.
- 산출물 재발견이나 문맥 캡처가 실패하면 이전 Excel 세션을 `ready` 상태로
  유지하고 새 세션으로 일부만 전환하지 않는다.
- 실제 Word 산출물을 새 편집 세션으로 전환해 경로·지문·세션 ID·상태를
  확인했고, 전체 자동 회귀 866개 중 863개 통과(unit 329·integration 331·
  windows 203), 외부 AI 라이브 3개 skip, 실패 0을 확인했다.

## 2026-07-18 — 최근 검증 산출물 열기·전면 포커스

- 연결된 Excel에서 `방금 만든 보고서 열어줘`, `최근 만든 발표자료 보여줘`를
  가장 최근 완료 워크플로의 검증 산출물에 연결한다. 실패 중인 더 최신
  워크플로는 대상을 가리지 않으며, 같은 초에 끝난 작업도 파일 수정 시각으로
  순서를 확정한다.
- 저장된 단계의 정확한 경로와 SHA-256 파일 지문이 현재 파일과 모두 일치할
  때만 연다. 원본 Excel이 바뀌었거나 산출물이 이동·수정·삭제됐으면 차단하고,
  Word와 한글 보고서가 함께 있으면 형식을 명시하도록 요청한다.
- 열린 앱·경로·지문을 다시 확인한 뒤 해당 문서 창을 최상위 창으로 정규화해
  전면 포커스를 검증한다. PowerPoint의 COM HWND 거부 환경에서는 정확한 문서
  경로를 먼저 확인하고 문서명과 일치하는 `PPTFrameClass`가 정확히 하나일
  때만 사용한다.
- 소유 Stage 11 fixture에서 최근 Word와 PowerPoint를 각각 다시 열어 정확한
  파일·앱·지문·전면 포커스 및 생성 프로세스 정리를 실제 검증했다.
- 전체 자동 회귀 862개 중 859개 통과(unit 327·integration 329·windows 203),
  외부 AI 라이브 3개 skip, 실패 0을 확인했다.

## 2026-07-18 — 현재 Excel 지시어 기반 복합 업무 생략 해석

- 편집 모드에서 Excel이 연결된 경우 `이거 보고서랑 발표자료 만들어줘`처럼
  분석 대상을 생략한 요청을 현재 연결 Excel 전체의 읽기 전용 분석으로
  해석한다. `이거`만으로 실행하지 않고 보고서·발표자료·생성 동사가 함께
  있어야 하며, 질문형 문장과 Excel 이외 문맥은 실행하지 않는다.
- `현재 엑셀을 한글 보고서와 발표자료 7장으로 정리해줘`처럼 현재 요청에 적은
  보고서 형식과 장수를 일회성 계획에 반영한다. 7장 같은 값이 장기 PPT 선호
  증거로 잘못 들어가지 않도록 10단계 업무 라우팅을 11단계 학습보다 우선한다.
- 미리보기에는 `현재 연결 Excel 전체 읽기 전용 분석`이라고 범위를 명시하고,
  기존의 원본 fingerprint 재확인·새 산출물·전체 승인·단계별 검증 계약을
  그대로 적용한다. 실제 Office Stage 11 probe에서도 문맥 라우팅을 확인했다.
- 전체 자동 회귀 850개 중 847개 통과(unit 317·integration 327·windows 203),
  외부 AI 라이브 3개 skip, 실패 0을 확인했다.

## 2026-07-18 — 승인형 복합 업무 스킬 재사용

- 검증 완료한 Excel 분석 → Word/한글 보고서 → PowerPoint 워크플로에서
  보고서 형식·PPT 장수·허용 단계 순서만 내용 없는 재사용 후보로 추출한다.
  원본/산출물 경로, 문서 내용, 공통 업무 데이터는 스킬 저장소에 넣지 않는다.
- `이 워크플로 기억해`를 별도 미리보기와 승인으로 활성화하고,
  `지난번처럼 해줘`를 현재 연결 Excel의 새 계획으로 해석한다. 후보 활성화를
  취소하면 재사용은 비활성 상태로 남는다.
- 재생할 때마다 현재 Excel fingerprint, 현재 승인 선호, 새 산출물 경로와
  충돌을 다시 확인하고 전체 고위험 미리보기를 승인받는다. 단계별 파일 지문과
  재열기 검증, 실패 단계 재개 계약을 그대로 사용하며 기존 산출물을 덮어쓰지 않는다.
- `지난번처럼 한글 보고서와 7장짜리 PPT로 해줘`처럼 현재 요청에 명시한 형식·
  장수는 기억한 구조보다 우선하고 장기 PPT 선호 증거로 잘못 기록되지 않는다.
- 소유 Stage 11 fixture에서 첫 Word·7장 PPT 생성 → 구조 후보 → 승인 →
  두 번째 새 Word·7장 PPT 재생·재열기를 통과했다. Excel 원본과 첫 산출물
  지문은 그대로였고 Goal 게이트는 내용 없는 저장·새 경로·실제 재생을 요구한다.
- 전체 자동 회귀 848개 중 845개 통과(unit 316·integration 326·windows 203),
  외부 AI 라이브 3개 skip, 실패 0을 확인했다.

## 2026-07-18 — 승인 선호 충돌 설명·교체 계약

- 이미 승인된 기본값과 다른 증거가 들어오면 기존 값은 계속 적용하고, 새 값이
  전체 증거의 75% 이상이면서 최소 3회 반복된 경우에만 교체 후보로 승격한다.
  승인 전에는 새 후보가 실제 업무 기본값을 바꾸지 않는다.
- 교체 미리보기는 현재 승인값, 새 후보값, 동일/전체 증거 수, 신뢰도와
  `승인 시 교체·취소 시 기존값 유지`를 함께 표시한다. 승인 결과도 신규
  활성화와 기존값 교체를 구분한다.
- 교체 후보와 반대되는 새 문장은 그 후보를 승인할 수 없도록 관찰값과
  제안값의 일치를 다시 확인한다. 사용자가 교체를 취소하면 후보만 기각하고
  기존 승인값을 유지하며, 새 증거가 3회 더 쌓여야 다시 묻는다.
- 소유 Stage 11 fixture에서 기존 격식체 유지 → 친근한 문체 교체 후보 설명 →
  취소 뒤 격식체 유지 → 새 증거 3회 → 승인 교체와 이전값 기록을 검증했다.
  Goal 게이트는 이 충돌 설명·취소·교체 증거를 필수로 요구한다.
- 전체 자동 회귀 840개 중 837개 통과(unit 311·integration 323·windows 203),
  외부 AI 라이브 3개 skip, 실패 0을 확인했다.

## 2026-07-18 — Word 접힌 Range 직접 교정 관찰

- Word 선택 문장 교체 결과는 실행이 검증한 Range 시작점, 새 텍스트 지문·길이·
  말투와 실행 직후 굵기·글자 크기·문단 정렬을 후속 문맥에 남긴다. 실행 뒤
  Office가 선택을 접어도 선택 밖 문장을 다시 읽지 않는다.
- Word는 빈 선택에서 문단 기호 한 글자를 돌려줄 수 있으므로 텍스트 길이가
  아니라 `Selection.Start == Selection.End`를 접힌 Range의 구조적 기준으로
  사용한다. 같은 문서 fingerprint와 교체 시작점이면 최대 5분만 관찰을
  유예하고, 사용자가 같은 Range를 다시 선택해야 직접 교정을 비교한다.
- 재선택한 텍스트 지문이 같고 굵기·글자 크기·문단 정렬 중 한 축만 바뀐
  경우에만 파일 범위 선호 증거를 한 번 기록한다. 다른 시작점·문서, 여러 축
  변경, 혼합 값은 기존 규칙대로 제외한다.
- 소유 임시 Word 문서에서 선택 교체 → 접힌 Range 확인 → 동일 Range 재선택 →
  직접 굵기 변경 → 증거·후보 1회 기록 → 기존 크기 편집·저장·read-back을
  통과했다. Goal 게이트는 Word와 PowerPoint 실제 대상에서 각각 접힌 Range와
  단일 Shape 직접 교정 관찰 증거를 요구한다.
- 전체 자동 회귀 834개 중 831개 통과(unit 307·integration 321·windows 203),
  외부 AI 라이브 3개 skip, 실패 0을 확인했다.

## 2026-07-18 — 한글 직접 서식 교정 관찰

- 한글 선택 영역의 `HCharShape` 굵기·Height와 `HParaShape.AlignType`을 읽어
  내용 원문 없이 굵기·글자 크기·문단 정렬 스냅샷으로 정규화한다.
- 텍스트 교체 뒤 선택이 커서로 접혀도 검증된 교체 전 시작 좌표, 새 텍스트
  지문·길이·말투, 실행 직후 서식값을 후속 문맥에 남긴다. 사용자가 같은 범위를
  다시 선택한 상태가 아직 바뀌지 않았으면 최대 5분만 관찰을 유지한다.
- 같은 문서 fingerprint와 한글 선택 시작 좌표에서 텍스트 지문이 같고 서식 한
  축만 바뀐 경우에만 파일 범위 선호 증거를 기록한다. 혼합·미정 값이나 여러 축
  동시 변경은 기존 규칙대로 해석하지 않는다.
- 소유 임시 한글 문서에서 선택 문장 교체 → 동일 범위 재선택 → 직접 굵기 변경
  → 증거 1회 기록 → 학습된 글자 크기 적용·read-back의 전체 흐름을 통과했다.
  Goal 게이트는 이 실제 관찰 증거도 필수로 요구한다.
- 전체 자동 회귀 831개 중 828개 통과(unit 304·integration 321·windows 203),
  외부 AI 라이브 3개 skip, 실패 0을 확인했다.

## 2026-07-18 — PowerPoint 단일 Shape 직접 교정 관찰

- JARVIS가 단일 PowerPoint Shape 전체 텍스트를 검증 편집한 직후, 같은 문서
  fingerprint·SlideID·ShapeID의 단일 Shape에서 사용자가 직접 한 축의 서식 또는
  명확한 축약·말투 교정을 했을 때 파일 범위 선호 증거로 기록한다.
- Shape 선택 문맥에서 이미 읽는 전체 텍스트의 지문·길이와 굵기·크기·정렬
  스칼라만 비교하며 원문은 학습 저장소에 남기지 않는다. 여러 Shape, 다른
  Slide/Shape, 혼합 서식은 관찰하지 않는다.
- 같은 Shape의 빈 텍스트 커서가 활성화된 동안에는 본문을 추가로 읽지 않고
  최대 5분 동안만 관찰을 유예한다. 다시 단일 Shape가 선택돼야 비교하며,
  다른 대상으로 이동하면 기존 관찰 기회를 폐기한다.
- 소유 임시 PowerPoint에서 제목 교체 후 직접 굵기 변경을 재현해 단일 증거 기록,
  기존 이동·앞 슬라이드 스타일 복사, 프로세스 정리를 함께 통과했다. Goal
  게이트는 Word·PowerPoint 두 실제 대상과 이 read-back 증거를 모두 요구한다.
- 전체 자동 회귀 828개 중 825개 통과(unit 301·integration 321·windows 203),
  외부 AI 라이브 3개 skip, 실패 0을 확인했다.

## 2026-07-18 — 한글 일반 편집 서식 기본값 적용

- 사용자가 승인한 굵기·글자 크기·문단 정렬 선호를 한글 일반 편집의
  `강조 방식 맞춰줘`, `글자 크기 맞춰줘`, `정렬해줘`처럼 값 하나가 생략된
  요청에만 보완한다. 사용자가 값을 명시하면 현재 요청이 우선한다.
- 한글 `크게`·`작게`는 현재 선택에서 ±2pt로 제한하고, 준비 단계에서 계산한
  절대 크기를 승인 뒤 실행해 재준비 과정의 이중 증감을 막는다.
- 소유 임시 한글 문서에서 학습된 `작게` 기본값의 미리보기·승인·실행과 정확한
  2pt 감소 COM read-back을 통과했다. 기존 Excel 편집도 같은 Stage 5 probe에서
  다시 통과했다.
- 제품 Goal 게이트가 Stage 5의 Excel·한글 두 대상을 모두 요구하고, 한글 결과의
  학습 기본값 read-back 증거가 없으면 통과하지 않도록 보강했다.
- 전체 자동 회귀 821개 중 818개 통과(unit 294·integration 321·windows 203),
  외부 AI 라이브 3개 skip, 실패 0을 확인했다.

## 2026-07-18 — PowerPoint 생성 서식 선호 적용·재열기 검증

- 승인된 굵기·글자 크기·문단 정렬을 새 PowerPoint의 모든 제목·본문 범위에
  구조적으로 적용한다. 글자 크기는 제목/본문 역할에 따라 크게 36/24pt,
  작게 24/14pt로 제한한다.
- 각 TextRange에 값을 쓴 직후 COM 값으로 다시 읽고, 적용 선호와 서식 범위 수를
  단계 검증 상태에 저장한다.
- 실제 5장 PowerPoint를 저장 후 별도 인스턴스로 다시 열어 제목·본문 10개 범위의
  굵기·크기·정렬과 슬라이드 수가 유지되는지 확인했다.
- 전체 자동 회귀 818개 중 815개 통과(unit 293·integration 319·windows 203),
  외부 AI 라이브 3개 skip, 실패 0을 확인했다.

## 2026-07-18 — Word·한글 동시 보고서 4단계 워크플로

- `Word와 한글 보고서`를 함께 요청하면 Excel을 한 번 읽고 Word, 한글,
  PowerPoint를 독립 단계로 생성·검증하는 저장 스키마 v3을 추가했다.
- Word 성공 뒤 한글이 실패하면 검증된 Word는 유지하고 한글 단계부터 재개하며,
  두 보고서 뒤 PPT가 실패하면 PPT만 재개한다. 산출물마다 별도 파일 지문과
  단계 후조건을 저장한다.
- 기존 v1·v2 한글 상태의 `create_word_report` 단계·성공 목록·실패 위치·검증
  결과를 명시적 `create_hwp_report` 단계로 이관해 재개 호환성을 보존한다.
- 승인 미리보기는 세 경로와 예상 변경 3개를 표시하고, 승인 뒤 형식·단계 순서·
  확장자·충돌을 다시 검증한다. 기존 파일이나 사용자가 바꾼 산출물은 덮어쓰지
  않는다.
- 실제 소유 2시트 Excel→Word→한글→PowerPoint 실행에서 내용·서식 read-back,
  PPT 5장, 세 산출물 지문, 원본 불변, 4단계 상태와 프로세스 정리를 통과했다.
  probe 시간 초과 진단에는 worker의 마지막 진행 단계를 남긴다.
- 전체 자동 회귀 817개 중 814개 통과(unit 292·integration 319·windows 203),
  외부 AI 라이브 3개 skip, 실패 0을 확인했다.

## 2026-07-18 — 제품 Goal 자동 수용 게이트

- 자연어, 현재 문맥, 네이티브 안전, 성공 재사용, 다중 앱 업무, 사용자 학습,
  실패 분류, 비숙련자 접근성의 8개 Goal 축을 전체 회귀와 Stage 5~12 소유 문서
  probe 증거에 연결했다.
- Word·한글 워크플로 probe를 별도 결과로 요구하며, 한 앱이라도 건너뛰거나
  결과가 7일보다 오래됐거나 안전·후조건 검사가 거짓이면 자동 수용을 거부한다.
- 실제 화면 읽기 프로그램, 음성 입력, 비숙련자 관찰은 자동 테스트로 통과시키지
  않고 `automated_pass_manual_pending`으로 분리한다. 사용자가 명시적으로 완료한
  세 항목만 최종 `accepted` 판정에 반영한다.
- 수용 보고서에는 상태·probe ID·시각·Git 신원만 남기며 사용자 문서 경로·내용과
  수동 시험 메모는 복사하지 않는다. 이 과정은 EXE를 만들지 않는다.
- 전체 자동 회귀 811개 중 808개 통과(unit 287·integration 318·windows 203),
  외부 AI 라이브 3개 skip, 실패 0을 확인했다.

## 2026-07-18 — 한글 보고서 문서 워크플로

- Excel 읽기 전용 분석 결과를 명시적 요청에 따라 Word `.docx` 또는 한글 `.hwp`
  보고서로 만들고 PowerPoint 요약과 함께 검증하는 형식 계약을 추가했다.
- `한글`·`HWP`·`HWPX` 요청은 `.hwp` 산출물로 정규화하며, 형식을 말하지 않은
  일반 `보고서` 요청은 기존과 같이 Word를 사용한다. Word와 한글을 동시에
  요청하면 임의로 하나를 선택하지 않고 다시 선택하도록 안내한다.
- 한글 보고서는 JARVIS 소유 `HwpObject`에 구조적 텍스트를 삽입하고, 승인된
  굵기·10/14pt 크기·문단 정렬을 COM 값으로 재확인한 뒤 기존 파일을 덮어쓰지
  않고 저장한다. 내용 read-back과 파일 SHA-256도 단계 검증 상태에 남긴다.
- 승인 뒤 보고서 형식이나 확장자가 바뀌면 첫 단계 전에 차단하며, 기존 저장
  상태의 `create_word_report` 단계 이름은 재개 호환성을 위해 유지한다.
- 실제 소유 임시 Excel→한글→PowerPoint와 Excel→Word→PowerPoint를 각각 실행해
  보고서 내용·서식, PPT 5장, 원본 불변, 단계 상태, 소유 프로세스 정리를 확인했다.
- 전체 자동 회귀 806개 중 803개 통과(unit 282·integration 318·windows 203),
  외부 AI 라이브 3개 skip, 실패 0을 확인했다.

## 2026-07-18 — 비숙련자용 키보드·모달 접근성

- 화면에서 숨긴 모드 라디오를 키보드 포커스 가능한 방식으로 바꾸고 좌우·상하
  방향키와 Home·End로 명령·질문·편집 모드를 결정적으로 전환한다.
- 대화 기록과 편집 파일 선택을 키보드 버튼으로 제공하고, 대화 영역을 ARIA log로
  노출하며 아이콘 버튼·입력·확인 카드에 보조기기용 이름과 구조를 추가했다.
- 주요 모달에 dialog 의미, 포커스 진입·Tab 순환·Esc 닫기·원래 버튼 복귀를
  적용했다. 로컬 브라우저 접근성 트리에서 방향키 전환과 AI 설정 포커스 흐름,
  콘솔 오류 0건을 실제 확인했다.
- 전체 자동 회귀 801개 중 798개 통과(unit 278·integration 317·windows 203),
  외부 AI 라이브 3개 skip, 실패 0을 확인했다.

## 2026-07-18 — 다중 Excel 시트 문서 워크플로

- Excel → Word → PowerPoint 승인형 워크플로가 첫 시트뿐 아니라 표시 시트 최대
  20개를 공통 업무 모델로 분석한다. 숨김 시트는 제외하고 표·지표·차트에 원본
  시트 이름을 보존한다.
- COM 값을 읽기 전에 시트별 50,000셀·30열과 전체 100,000셀 한도를 검사한다.
  보고서 미리보기 행은 시트별 100행·전체 500행으로 제한한다.
- JARVIS 소유 임시 2시트 Excel로 분석, Word 저장·재열기, PowerPoint 5장
  저장·재열기, 원본 불변, 단계 상태와 Office 프로세스 정리를 실제 검증했다.
- 전체 자동 회귀 796개 중 793개 통과(unit 273·integration 317·windows 203),
  외부 AI 라이브 3개 skip, 실패 0을 확인했다.

## 2026-07-18 — UI Automation 실행 전 1회 대상 재탐색

- `uia_click`·`uia_set_text`가 컨트롤을 못 찾았고 외부 동작이 시작되지 않은
  경우에만 공통 `PreExecutionRecoveryContract`로 캐시를 비운 뒤 한 번 새로
  탐색한다.
- 앱·selector를 해시한 대상 서명을 유지하고, 첫 탐색에서 창을 확인했다면 같은
  handle·process·제목 digest의 창만 허용한다. 다른 창은 클릭·입력 전에
  `context_changed`로 차단한다.
- 재탐색 뒤에도 없으면 1회 소진으로 종료하고, 모호한 후보는 기존 선택 확인으로
  넘긴다. 실제 click·focus·입력이 시작된 뒤의 오류는 자동 재실행하지 않는다.
- 전체 자동 회귀 791개 중 788개 통과(unit 268·integration 317·windows 203),
  외부 AI 라이브 3개 skip, 실패 0을 확인했다.

## 2026-07-18 — 명령 참조 문맥과 학습 스킬 대상 계약

- 일반 명령 모드에서 사용하지 않던 `conversation_state`를 최근 3회 구조화 결과로
  연결했다. `방금·아까·지난번·그대로·다시` 같은 현재 참조 표현이 있을 때만 이전
  요청과 action/target/app/macro/status/verified를 후보 탐색과 AI prompt에 쓴다.
- 관계없는 명령에는 이전 대상을 전혀 넣지 않으며, 코드·행동 계획·임의 필드는
  참조 문맥에서 제거한다. 세션 전환·저장·새 대화와 확인 응답도 분리했다.
- 특정 앱에 등록된 action/native/UIA 스킬의 실제 계획 대상이 다른 앱이면 첫
  외부 변경 전에 차단한다. 여러 앱 계획은 `시스템` 스킬만 허용한다.
- 전체 자동 회귀 786개 중 783개 통과(unit 268·integration 317·windows 198),
  외부 AI 라이브 3개 skip, 실패 0을 확인했다.

## 2026-07-18 — 연결 문서 재탐색 복구

- 명시적 편집 요청의 문맥 캡처가 연결 문서를 찾지 못하면
  `connected_document_rediscovery` 전략이 공통 실행 전 복구 계약 아래에서
  열린 문서 컬렉션·ROT·Excel 창을 한 번만 다시 읽어 같은 경로의 문서를 찾는다.
- 탐색 전에 파일 신원 fingerprint를 재계산해 교체·삭제된 파일은
  `target_changed`로 차단한다. 세션 검증과 캡처 사이 race에 대한 이중 방어다.
- 같은 문서를 찾으면 세션의 창 handle만 재바인딩한다
  (`EditSessionManager.rebind_window_handle`). 문서 상태와 편집 상태 기계는
  변경하지 않으며, 재캡처한 fingerprint가 요청과 같아야 계속한다.
- 못 찾으면 `not_found`로 종료하고 문서를 다시 열어 연결하라고 안내한다.
  닫힌 문서를 대신 열거나 창 focus를 바꾸지 않는다. Office 입력 중이면
  `unavailable`(재시도 가능)로 분류한다.
- 미저장 Excel(runtime) 세션과 상태 조회·polling은 재탐색 대상이 아니다.
- UI Automation 학습 행동의 대상 재탐색은 검토 후 다음 후보로 남겼다.
- 전체 자동 회귀 763개 중 760개 통과(unit 260·integration 302·windows 198),
  외부 AI 라이브 3개 skip, 실패 0을 확인했다.

## 2026-07-18 — 직접 수정 관찰 확장 (서식·말투)

- JARVIS 검증 편집 직후의 직접 수정을 한 사건당 하나의 관찰로 분류한다.
  우선순위는 축약 > 말투 > 서식이며 모호한 사건은 해석하지 않는다.
- 문장 어미 기반 결정적 말투 분류(합쇼체·해요체·해라체)를 추가하고, 비슷한
  길이의 재작성에서 명확한 어미 전환만 `격식체`·`친근한 문체` 증거로 기록한다.
  저장 값은 어미 분류 한 단어이며 문장 원문은 남기지 않는다.
- 텍스트 지문이 같고 굵기·글자 크기(1pt 이상)·문단 정렬 중 정확히 하나만 바뀐
  Word·PowerPoint 직접 수정을 `텍스트 강조 방식`·`글자 크기 방향`·`문단 정렬
  기본값` 증거로 기록한다. 혼합·미정 native sentinel과 다중 변경은 제외한다.
- 증거 3회 뒤 안내되는 `앞으로도 …해줘` 확인 문장이 현재 파일에 대기 중인
  같은 값의 후보를 실제로 활성화하도록 연결했다. 이전에는 안내 문구가 무관한
  전역·업무 후보를 새로 만드는 공백이 있었다.
- `앞으로`·`항상` 같은 일반화 표현이 있을 때만 서식 선호 명시 문장을 해석하고,
  `가운데 정렬로 해줘` 같은 일반 명령은 문서 편집으로 남긴다.
- 커서로 접힌 선택의 앱별 증명을 연구해 Goal 문서에 기록했다. 세 앱 모두 현재
  개인정보 경계 안에서는 구현하지 않으며 PowerPoint shape 전체 텍스트 지문
  방식을 후속 후보로 남겼다.
- 전체 자동 회귀 755개 중 752개 통과(unit 259·integration 295·windows 198),
  외부 AI 라이브 3개 skip, 실패 0을 확인했다.

## 2026-07-18 — 실행 전 공통 복구 계약·연결 문서 focus 복구·교정 학습 확장

- 실행 전임을 증명할 수 있고 대상 서명이 같을 때만 최대 1회 허용하는 공통 복구
  계약(`engine/recovery`)을 추가했다. 복구 증거에는 해시·불리언·횟수만 남기고
  앱 이름·경로·사용자 원문은 저장하지 않는다.
- 등록되지 않은 앱 열기·닫기 대상은 실행 전에 Windows App Paths와 시작 메뉴·
  바탕화면 바로가기를 한 번만 재탐색하고, 미발견 시 반복 탐색 없이 사용자에게
  묻는다.
- 명시적 편집 요청의 준비·승인 직전에 연결 문서가 다른 창 뒤로 밀린 경우 그
  문서 창만 한 번 활성화하고 동일 문서 fingerprint를 재확인한 뒤에만 미리보기와
  승인 실행을 계속한다. 평소 상태 조회는 창 focus를 바꾸지 않는다.
- 실패 결과를 결정적 책임 분류(runtime_recoverable, needs_input,
  missing_capability, implementation_bug, environment_blocked, policy_blocked,
  preference_mismatch)로 나누는 failure triage와 익명 개발 이슈 기록을 추가했다.
- 미리보기 재작성·취소 사유와 검증된 후속 교정, JARVIS 편집 직후 같은 구조적
  시작점의 명확한 직접 축약을 파일 범위 선호 학습 증거로 구조화했다. 같은 값
  3회와 사용자 승인이 있어야 기본값으로 활성화된다.
- 편집 대상 표시 bar와 선택 overlay UI를 추가해 현재 편집 대상과 선택 영역을
  화면에서 확인할 수 있게 했다.
- 앱 실행 차단 정책을 `engine/security/launch_policy`로, 시스템 볼륨 제어를
  `engine/system_volume`으로 분리했다.
- 전체 자동 회귀 729개 중 726개 통과, 외부 AI 라이브 3개 skip, 실패 0을
  확인했다.

## 1.1.0-rc.6 — 독립 감사 후속 안전성 보완

- 편집 문맥의 1초 COM polling을 제거하고 연결·탭 진입·focus·수동 새로고침·
  명령 전·처리 후 이벤트에서만 갱신하도록 바꿨다.
- 편집 전 문맥 갱신을 await하고 요청 `context_fingerprint`와 prepare 시점 문맥이
  다르면 미리보기를 만들기 전에 차단한다.
- 새 문서 workflow는 승인 전 영구 JSON을 만들지 않고 승인 순간 원본·출력 충돌을
  다시 검사한 뒤 최초 저장한다. 취소·승인 대기 상태는 재개 대상에서 제외했다.
- PPT 출력명에 실제 장수를 반영하고 Word 보고서에는 원본 basename만 넣는다.
- 11단계 범위를 명시적 사용자 선호 학습 v1로 정확히 한정했다.
- VBA 프로시저 호출 반환과 셀·파일 등 업무 결과 검증을 별도 상태로 구분했다.
- clean commit 전용 `git archive` 소스 공유 스크립트와 export 제외 규칙을 추가했다.
- 개인 PC 런타임 절대 경로를 문서에서 제거하고 과거 감사의 대상 버전을 명시했다.
- 전체 자동 회귀 632개 중 629개 통과, 외부 AI 라이브 3개 skip, 실패 0개를 확인했다.
- 8단계 네 앱 각 100/100, 9단계 실제 VBA 백업·수정·복원·실행, 10·11·12단계
  전용 프로브를 모두 다시 통과했다.
- 이 Office 빌드에서 VBA 프로젝트 접근 뒤 Excel `Quit()`가 지연되는 경우를 위해,
  검증 시작 전 존재하지 않았고 검증 중 생성된 Excel PID만 종료하는 소유 인스턴스
  정리 폴백을 추가했다.
- Python 매크로 timeout은 실제 자식 프로세스 생성 뒤부터 계산하고, 임시파일 준비
  중 취소가 도착하면 프로세스를 새로 만들기 직전에 다시 차단하도록 보강했다.
- 실제 Eel 브리지를 띄운 GUI에서 편집 모드 전환, 문맥 수동 새로고침과 연결 전
  안내 상태를 확인했고 현재 페이지 콘솔 오류 0건을 기록했다.

## 2026-07-17 — Prototype 1.0 8단계·Prototype 1.1 9단계 안정화 재검

- PowerPoint 선택 객체를 `SlideID`·`ShapeID`로 다시 획득하고 이동·크기 변경의
  쓰기, 재읽기, 복원 결과를 새 COM 프록시로 검증하도록 보강했다.
- 끊어진 PowerPoint COM 프록시는 쓰기 전까지만 제한 재시도하고, 쓰기 이후에는
  중복 편집을 막기 위해 재시도하지 않는다.
- PowerPoint 보완 후 100회 실검을 다섯 회 수행해 누적 500/500, 각 회 COM
  interface 증가 0, 적용·복원·프로세스 정리를 확인했다.
- 한글 실검을 JARVIS 소유 전용 미저장 문서에서 수행하고 worker 진행 단계와
  timeout 진단을 남기도록 바꿨다. 네 앱 전체 100회 실검이 통과했다.
- Excel VBA 신뢰 설정을 읽기 전용으로 진단하고 `VbaTrustAccessBlocked`로
  일반 오류와 명확히 구분하며 실패 단계·재시도 여부·진단 문맥을 API까지 전달한다.
- VBA 정적 분석을 실제 프로젝트 접근보다 먼저 실행하고, 쓰기와 복원이 모두
  실패해도 외부 `.bas` 백업을 보존하도록 검증했다.
- 전체 자동 회귀 623개 중 620개 통과, 외부 AI 3개 skip, 실패 0개를 확인했다.
- 실제 VBA 프로젝트 접근은 이 PC의 Excel 신뢰 설정으로 차단됐고, 설정을
  변경하거나 우회하지 않았다.

## Prototype 1.1 12단계 - 자가 진단·안전한 개선 제안

- 실패 실행을 개인정보가 제거된 사건으로 수집하고 같은 구조 실패를 fingerprint로
  그룹화하는 `DiagnosticIncidentManager`를 추가했다.
- 실패 단계·기대/실제 상태·앱 버전·원인 후보·합성 재현 규격·필수 회귀 테스트를
  한 보고서로 조회하도록 했다.
- 성공·취소·확인 대기·busy와 진단 자체의 빈 결과는 사건에서 제외했다.
- 사용자 명령·경로·문서 내용·오류 문장을 저장하지 않고 구조 식별자·확장자·
  해시만 보존하도록 했으며 기존 실행 진단과 백업도 익명 형식으로 마이그레이션한다.
- “최근 오류 진단해줘” 자연어 명령과 사건 목록·보고서·상태·건강 요약·수정 제안
  API를 추가했다.
- 수정 제안은 사용자 승인, 분리 worktree, 테스트 우선, 전체 회귀, 별도 빌드 승인,
  기존 실행본 롤백 경계를 명시하며 어떤 단계도 자동 실행하지 않는다.
- 합성 실패 주입에서 사용자 문서·Office 앱·worktree·빌드 없이 개인정보 부재와
  실행 바이너리 불변을 검증했다.
- 12단계 추가 후 전체 자동 회귀 614개 중 611개 통과·외부 AI 3개 skip·실패
  0개를 확인했다.

## Prototype 1.1 11단계 - 명시적 사용자 선호 학습 v1

- 사용자 업무 선호를 기존 실행 방식 선호와 분리해 저장하는
  `UserPreferenceLearningManager`를 추가했다.
- 1회 관찰, 3회 반복 후보 승격, 사용자 승인 후 활성화라는 보수적 학습 정책과
  중복 증거·상충 값·후보 취소 처리를 구현했다.
- 전역·앱·업무·파일 범위와 `파일 > 업무 > 앱 > 전역` 우선순위를 추가했다.
- 요약 길이, 문체, 제목, 숫자, 표, PPT 장수, 파일 위치, 확인 작업, 작업 순서,
  VBA 수정 패턴을 허용 목록과 값 범위로 제한했다.
- 승인된 선호를 10단계 Word/PPT 생성 기본값에 적용하고, 현재 명령의 명시적
  값이 학습값보다 우선하도록 했다.
- Word 세션 학습, 후보 승인·취소, 7장 PPT 기본값, 명시적 5장 우선, VBA 패턴
  보완을 단위·통합 테스트로 검증했다.
- 소유 임시 Office 문서에서 승인 선호 재시작 유지와 실제 7장 PPT 생성을
  재열기 검증했으며 사용자 문서·선호 저장소를 변경하지 않았다.
- 11단계 추가 후 전체 자동 회귀 601개 중 598개 통과·외부 AI 3개 skip·실패
  0개를 확인했다.

## Prototype 1.1 10단계 - 앱 간 문서 워크플로

- 연결 Excel을 읽기 전용으로 분석해 공통 JSON 업무 데이터, Word 보고서,
  PowerPoint 5장 요약으로 잇는 `WorkflowExecutor`를 추가했다.
- 승인 전에는 산출물을 만들지 않고, 기존 파일은 고유 이름으로 피하며,
  Excel 원본 지문이 바뀌면 실행을 차단한다.
- 현재 단계·성공 단계·실패 단계·생성 파일·파일 지문·검증 결과를 원자적으로
  저장하고 실패 단계만 재시도한다.
- Word 생성 후 PPT 실패를 주입해 재개 시 Excel 분석과 Word가 중복 실행되지
  않는 단위·통합 테스트를 추가했다.
- JARVIS 소유 임시 Excel·Word·PowerPoint로 보고서 재열기, PPT 정확히 5장,
  원본 불변, 프로세스 정리를 실제 검증했다.
- 10단계 추가 후 전체 자동 회귀 581개 중 578개 통과·외부 AI 3개 skip·실패
  0개를 확인했다.

## 개발 중 — Prototype 1.0·1.1 편집모드

- `v1.1.0-rc.5`에서 `feature/prototype-1.0-edit-mode` 브랜치를 분리했다.
- Excel·한글·Word·PowerPoint의 COM 등록, 앱 버전, 선택 읽기, 임시 편집,
  Undo, 저장 재개봉, 소유 프로세스 정리를 확인하는 1단계 검증 도구를 추가했다.
- 기존 사용자 앱이 실행 중이면 실제 앱 검증을 거부하고, JARVIS가 생성한 빈
  임시 문서만 사용하는 보호 경계를 추가했다.
- COM 변경이 앱의 네이티브 Undo 이력에 항상 등록되지 않는 점을 반영해,
  네이티브 Undo와 변경 전 스냅샷 복원을 구분해 검증하도록 했다.
- 편집 요청·준비 작업·실행 결과를 COM-free JSON 계약으로 정의하고, 앱
  어댑터 허용 작업만 준비·실행·검증·복구하는 공통 coordinator를 추가했다.
- 편집 세션의 연결·준비·승인·실행·검증·커밋·복구 상태와 revision 충돌
  검사를 추가했다.
- `edit` 요청을 기존 명령 파이프라인과 분리하고, 세션 정보가 없거나 아직
  핸들러가 연결되지 않았으면 실행하지 않도록 했다.
- 알 수 없는 모드의 명령 폴백과 질문·대화모드의 앱 실행을 백엔드에서
  차단하는 모드 권한 정책을 추가했다.
- GUI에 `명령 | 질문 | 편집` 탭과 파일 선택·드롭, 열린 문서 연결, 연결 해제,
  자동 창 배치 컨트롤을 추가했다.
- Excel·한글·Word·PowerPoint 지원 확장자와 앱 설치 여부를 확인하고, 열린
  네이티브 문서의 전체 경로가 일치할 때만 세션을 만드는 `FileIntakeManager`를
  추가했다.
- 세션에는 COM 객체 대신 파일 경로·앱 유형·파일 식별 fingerprint·선택 참조만
  저장하고, 파일 교체나 다른 세션 요청을 `stale_context`로 차단한다.
- 파일 경로 ROT moniker fallback을 추가해 Office Application이 ROT에 늦게
  등록되는 경우에도 선택한 Workbook·Document·Presentation을 정확히 찾는다.
- 문서 모니터 기준 68:32 자동 배치를 세션당 한 번만 시도하고, 배치 실패가
  문서 연결을 취소하지 않도록 격리했다.
- 실제 임시 Excel·Word·PowerPoint 파일의 연결·세션·프로세스 정리를 검증했고,
  실행 중인 사용자 한글 문서는 읽기 전용 세션 탐색만 수행해 보존했다.
- Excel·한글·Word·PowerPoint의 현재 컨테이너, 셀/문자 Range, 커서, 슬라이드,
  Shape를 읽기 전용으로 캡처하는 앱별 `ContextProvider`와 `EditContextManager`를
  추가했다.
- 파일 식별 `document_fingerprint`와 선택 상태 `context_fingerprint`를 분리하고,
  같은 선택은 안정적으로 유지하면서 선택 위치나 선택 내용이 바뀌면 문맥
  fingerprint가 달라지도록 했다.
- 연결 문서가 닫히거나 다른 문서가 활성화되거나 파일이 교체되면 문맥 조회와
  편집 요청을 `stale_context`로 차단한다.
- 편집 화면에 현재 시트·범위, 문장 Range, 슬라이드·Shape와 제한된 텍스트
  미리보기를 표시하고, 편집 탭이 보이는 동안 문맥을 자동 갱신한다.
- Excel·한글의 제한된 한국어 편집 의도를 기존 네이티브 PreparedAction으로
  변환하는 5단계 어댑터와 모든 쓰기의 변경 미리보기·승인 흐름을 추가했다.
- Excel 값·수식·합계·평균·서식·필터·정렬·찾기/바꾸기·행/열 삽입과 한글
  선택 교체·로컬 축약/격식체·글자/문단 서식·찾기/바꾸기를 지원한다.
- 승인 직전 4단계 문맥과 네이티브 fingerprint를 함께 재검증하고, 한글 선택이
  사라진 경우 커서 입력으로 바뀌지 않도록 전체 선택 digest와 길이를 확인한다.
- 취소·실패·오래된 승인 뒤 편집 세션을 `ready`로 정리하고, 실행 성공은
  네이티브 재읽기 검증이 통과한 경우에만 반환한다.
- Word 선택 Range 텍스트 교체, 굵기·글자 크기, 문단 정렬, Style·
  표 셀 조회와 저장을 승인·재읽기 검증 흐름으로 추가했다.
- PowerPoint 단일 Shape/텍스트 교체·서식·정렬·이동·크기 변경과
  Placeholder 식별, 앞 슬라이드 동일 역할 Shape 스타일 복사를 추가했다.
- Word는 문자 Range·전체 문서 digest·표 셀을, PowerPoint는 슬라이드 ID·
  Shape ID·선택 텍스트 digest를 확인 전·후 비교하며 불일치 시 차단한다.
- 직전 편집 후의 정확한 문맥에서만 `조금 더`, `너무 길어`, `다시`,
  `두 번째 문장만`, `마지막 문장은 원래대로`, `아까처럼` 같은 후속 명령을
  확장하고 같은 대상의 연속 편집 횟수를 세션에 기록한다.
- 텍스트 미리보기에 안전한 대안을 만들 수 있을 때 `다시 작성` 선택지를
  추가하고, 기존 준비 작업을 실행하지 않은 채 새 request ID의 미리보기로
  교체한다.
- Excel·한글·Word·PowerPoint의 직전 JARVIS 편집 한 건을 구조화된 원본
  스냅샷과 네이티브 Undo 보조 수단으로 복원한 뒤 다시 읽어 검증한다.
- 대상 변경이나 문서 종료 시 연속 수정·복원 기록을 폐기하고, 복원 성공
  뒤에도 기록을 비워 중복 복원을 차단한다.
- 한 controller에서 Excel→한글→Word→PowerPoint 대표 자연어 편집을
  순차 실행하고, 앱 전환 뒤 이전 세션 요청과 닫힌 문서의 후속 명령을
  차단하는 8단계 통합 회귀를 추가했다.
- 네 앱 각각 100회 연속 네이티브 편집, 읽기 전용·보호 문서, 같은 이름의
  다른 파일, 예외 뒤 세션 복구와 재시작 초기화를 검사한다.
- PowerPoint의 일시적인 COM busy·호출 거부·비활성 프레젠테이션 응답에만
  제한 재시도를 적용하고, Shape 선택 조회 오류가 다른 대상으로 우회되지
  않게 했다.
- JARVIS 소유 네 앱 임시 문서에서 앱별 100/100 편집, COM interface 증가 0,
  대표 자연어 적용·검증·복원과 소유 프로세스 정리를 실제 검증해
  `prototype_1_0_ready=true`를 확인했다.
- 8단계 전체 자동 회귀 555개 중 552개 통과·외부 AI 3개 skip·실패 0개를
  확인했다.
- `.xlsm/.xlsb` VBA 프로젝트·모듈·Sub·Function 목록, 단일 모듈 코드 읽기와
  로컬 구조·오류·위험 분석을 제공하는 9단계 Excel VBA 어댑터를 추가했다.
- 마지막 행 계산 보정, UsedRange의 Selection 제한, 원본 시트 백업 삽입,
  정확히 한 곳의 코드 조각 교체를 unified diff 미리보기로 준비한다.
- 표준 모듈 수정 전에 `%LOCALAPPDATA%\Jarvis\data\vba_backups`에 원본 `.bas`를
  내보내고, 변경 후 digest가 다르거나 예외가 발생하면 메모리 원문으로 복원한다.
- 매크로 실행을 코드 수정과 분리된 고위험 승인 작업으로 만들고, Shell·파일
  삭제·네트워크·레지스트리 기능이 감지되면 강한 경고와 두 번째 승인을 요구한다.
- 9단계 추가 후 전체 자동 회귀 572개 중 569개 통과·외부 AI 3개 skip·실패
  0개를 확인했다.
- JARVIS 소유 임시 `.xlsm` 실제 검증은 Excel 보안 센터의 VBA 프로젝트 개체
  모델 접근 설정에 의해 `blocked_vba_trust`로 안전하게 차단됐으며, 사용자
  문서 변경이나 좀비 Excel 프로세스는 없었다.
- JARVIS 소유 임시 Word 문서에서 선택 문장 5회 연속 축약, 선택 유지,
  연속 횟수 `1~5`, 직전 편집 복원과 세션 `ready` 복귀를 실제 검증했다.
- 7단계 전체 자동 회귀 540개 중 537개 통과·외부 AI 3개 skip·실패 0개를
  확인했다.
- 자동 회귀 525개 중 522개 통과·외부 AI 3개 skip·실패 0개를 확인했고,
  JARVIS 소유 네 앱 임시 문서에서 동일 선택 안정성과 선택 변경 감지를
  검증한 데 이어 Excel 값 입력·행 삽입과 한글 선택 문장 교체를 실제 승인
  흐름으로 검증했다. Word 문장 교체·글자 크기·저장과 PowerPoint
  제목 교체·Shape 이동·앞 슬라이드 스타일 복사도 실제 검증한 뒤 소유
  프로세스가 남지 않은 것을 확인했다.

## 1.1.0-rc.5 — 브라우저 폴백과 Office COM 생명주기

- Chrome, Edge, 시스템 기본 브라우저, 추가 설치 브라우저 순서의 GUI 실행
  폴백과 사용자 오류 안내, 선택·실패 로그를 추가했다.
- Excel/HWP COM 작업마다 `CoInitialize()`와 `CoUninitialize()`를 짝지어
  호출하고, 기존 인스턴스와 Jarvis 생성 인스턴스의 소유권을 분리했다.
- Jarvis 소유 문서와 Application만 정리하며, 예외가 발생해도 소유
  Application 종료를 시도하도록 공통 생명주기 객체를 추가했다.
- 동적 Office 코드에도 COM 초기화·해제와 사용자 인스턴스 보존 규칙을
  적용했다.

## 1.1.0-rc.4 — 빈 앱 사전과 클라우드 AI 단순화

- 최초 앱 사전을 완전히 비우고 사용자가 앱·최근 앱·북마크 스캔을 실행한 뒤에만
  항목이 등록되도록 기본 앱·웹사이트 자동 주입을 제거했다.
- Ollama 제공자, 로컬 모델 설정, 상단 전환 토글과 클라우드 실패 시 자동 대체 경로를
  제거했다. AI 기능은 OpenAI 또는 Gemini API 키가 있을 때만 동작한다.

## 1.1.0-rc.3 — 단일 실행형 재부팅 보정

- PyInstaller one-file 실행본이 재부팅할 때 이전 `_MEI` 임시 경로를 재사용하지 않도록
  새 압축 해제 환경을 요청한다.
- 친구 전달용 단일 EXE에는 빈 기본 기억·빈 설정만 포함하고 대화 세션을 포함하지 않는다.

## 1.1.0-rc.2 — 보정 릴리스 후보

### 보안과 결과 정확성

- `PyPDF2`를 `pypdf 6.14.2`로 교체하고 PDF 크기·페이지·추출 문자 상한 추가
- 시스템 종료 명령을 `subprocess.run(..., shell=False)`로 전환하고 실제
  반환 코드에 따라 성공·실패 보고
- runtime 소스의 bare `except`, 비의도적 `print()` 경로 정리
- 사전·학습 GUI의 동적 HTML 삽입을 안전한 DOM 조립과 숫자 검증으로 전환

### 구조와 재현성

- 실행 폴더와 개발 저장소를 분리하고 테스트·검증·빌드 도구 복원
- 명령 파이프라인을 대화·네이티브·학습·매크로·AI fallback route로 분리
- AI batch 실행을 dispatcher, action executor, 결과 aggregator로 분리
- confirmation 생성과 학습 행동 재실행을 parser 밖의 서비스로 분리
- Excel·HWP의 COM-free helper 및 Word·PowerPoint 확장 계약 추가
- 동결 EXE 검증 probe를 현재 confirmation 정책과 실행 결과 구조에 맞게 갱신

### 최종 검증

- 자동 테스트 435개: 432개 통과, 외부 AI 3개 skip, 실패 0개
- `pip-audit` 알려진 취약점 0건
- 실제 Excel 20/20, 한글 8/8 통과
- 후보 EXE 기동, frozen worker, timeout·취소·재시도·잠금, TLS, Excel COM 통과
- 기존 사용자 데이터 33개 파일 사전·사후 바이트 동일
- 태그 `v1.1.0-rc.2`, 빌드 commit `ceb6c17d14f495d167a3c12417464c124bde0d90`

## 1.1.0 — 2026-07-15 이전 실행본

- HTML 정화와 로컬 마크다운 렌더러
- 동시 명령 busy 보호와 실행 진단
- 학습 네이티브 실행 경로 보강
- Excel·한글 네이티브 편집, 확인·검증·복구 기반

이 빌드는 R6 교체 전 current였으며, 현재는 `JARVIS_RUNTIME\previous`에
직전 정상 백업으로 보존돼 있습니다.

## 1.0 — 2026-07-14

- Windows 앱·파일·웹·미디어 기본 명령
- AI 질문·대화와 동적 행동 안전 검사
- 사용자 사전·매크로·세션 저장 기반
