# Known limitations

- 일반 명령의 직전 참조는 같은 대화 세션에서 최근 3회까지이며 `방금`, `아까`,
  `지난번`, `그대로`, `다시`처럼 현재 문장에 참조 표현이 있을 때만 사용한다.
  코드·계획·문서 본문은 참조 상태에 넣지 않는다. 세 번보다 오래된 대상을
  자동 선택하거나 편집 모드 밖에서 현재 앱의 문서·선택을 추측하지 않으며,
  후보가 여러 개면 사용자의 설명이 필요하다.
- 재사용 스킬의 등록 앱·실제 대상 계약은 구조화된 action/native/UIA 계획에
  적용된다. 동적 Python 스킬은 코드·인자 fingerprint와 외부 상태 변경 preflight를
  사용하지만 코드가 다루는 모든 앱을 정적 대상 계약으로 완전히 증명하지는 못한다.
  여러 앱 구조화 계획은 `시스템` 스킬만 허용하며 단계별 UI 대상 재탐색은 아직
  아래 공통 복구 확장 과제로 남아 있다.

- 자율 복구는 현재 명시적인 앱 열기·닫기의 대상 재탐색, 명시적 편집 요청의 연결
  문서 focus·창 재탐색, 저장된 닫힌 문서의 정확 경로 1회 재열기만 다룬다. 앱
  탐색은 Windows App Paths와 시작 메뉴·바탕화면 바로가지만 한 번 확인하며 느린
  전체 Program Files 검색은 하지 않는다. 문서 창 재탐색은 실행 중인 앱의 문서
  목록·ROT·Excel 창만 읽는다. 여기서도 찾지 못한 저장 문서는 최초 편집 요청에서만
  파일 신원을 재검증한 뒤 정확한 경로를 한 번 열 수 있다. 상태 조회, 기존 미리보기
  승인, 실행 중 작업, 미저장 문서에는 재열기를 적용하지 않는다. 승인 중 문서가
  닫히면 그 미리보기를 폐기하고 새 요청을 요구한다. 모든 시도는 `실행 전`, 요청
  대상 서명 동일, 최대 1회라는 공통 복구 계약을 통과하며 파일 교체·삭제, 다른
  문서 확인, Windows focus 거부 시에는 변경 전에 중단한다.
- UI Automation 클릭·텍스트 입력은 컨트롤을 못 찾은 경우에만 공통 실행 전
  복구 계약으로 캐시를 비우고 한 번 새로 탐색한다. 첫 탐색에서 창을 확인했다면
  같은 handle·process·제목 지문의 창만 허용한다. 다른 창, 모호한 후보, 탐색
  제한은 실행 전에 멈추며 클릭·focus·입력이 시작된 뒤에는 자동 재탐색하지
  않는다. 앱 자동 재실행, 로그인·모달 우회, 이미 수행된 다단계 UIA 계획 전체의
  자동 반복은 지원하지 않는다. 미저장 Excel(runtime) 문서는 전용 창 토큰
  검증을 사용하므로 문서 재탐색 대상이 아니다.
- 12단계 책임 분류는 자율 복구 가능성을 구분하지만 모든 실패를 자동 재실행하지
  않는다. 실행 시작 여부와 대상 동일성을 증명할 수 없는 경우에는 중복 변경을
  막기 위해 사용자 안내 또는 개발 이슈 등록으로 종료한다.

- 12단계 원인 분석은 오류 유형·상태·실패 단계에 대한 결정적 후보 분류다. 문서
  내용과 전체 스택 추적을 보관하지 않으므로 모든 근본 원인을 자동 확정하지 않는다.
- 12단계 수정안은 제안만 만든다. 별도 worktree 코드 수정, 전체 회귀, 새 EXE
  빌드와 교체는 사용자 승인 없이 실행하지 않는다.
- 레지스트리에 등록되지 않은 포터블·가상화 Office 설치는 진단의 앱 버전이
  `null`로 표시될 수 있다.

- 11단계 사용자 선호 학습은 허용된 한국어 값과 구조화 가능한 교정만 인식한다.
  미리보기 재작성·취소 사유와 검증된 후속 교정을 증거로 수집한다. JARVIS 밖의
  직접 수정은 Word·한글·PowerPoint에서 JARVIS 편집 직후 같은 구조적 시작점이
  증명된 경우에만 한 사건당 하나의 관찰로 해석한다. 명확한 축약(기존의
  35~75%)은 `간결한 문체`, 비슷한 길이의 재작성에서 문장 어미 분류가 명확히
  바뀐 경우는 `격식체`·`친근한 문체`, 텍스트가 완전히 같고 굵기·글자 크기·문단
  정렬 중 정확히 하나만 바뀐 경우는 해당 서식 선호의 증거가 된다. Word는 문서
  내 시작 offset, PowerPoint는 SlideID·ShapeID·텍스트 시작점 또는 단일 Shape
  전체 텍스트 지문, 한글은 정규화한 선택 시작 좌표를 사용한다. 선택 시작점
  이동, Excel 셀 변경, 여러 서식이 함께 바뀐 수정, 혼합·불명 어미, 어미 밖의
  의미·어휘 변화, 10자 미만의 거의 전부 삭제는 자동 해석하지 않는다.
  Word는 검증된 Range 교체 뒤 `Start == End`인 같은 시작점의 접힌 선택을,
  PowerPoint는 단일 Shape 전체 편집 뒤 같은 Shape의 빈 텍스트 커서를,
  한글은 검증된 텍스트 교체 뒤 같은 시작 좌표의 접힌 커서를 각각 최대 5분만
  유예한다. 세 앱 모두 같은 구조적 범위를 다시 선택한 뒤에만 비교한다.
  자유로운 문장이나 문서 원문을 저장하거나 한 번의 교정으로 선호를 확정하지
  않는다.
- 승인된 기본값과 다른 값은 전체 구조화 증거의 75% 이상이면서 최소 3회
  반복돼야 교체 후보가 된다. 교체 전에는 기존 승인값을 유지하고, 승인
  미리보기에서 현재값·후보값·증거 수·신뢰도를 설명한다. 취소하면 후보만
  기각하고 기존값을 유지하며 새 증거 3회 뒤에만 다시 묻는다. 이 계약은 허용
  목록 값에만 적용되며 자유 문장 의미나 상황별 예외를 자동 일반화하지 않는다.
- 말투 관찰은 문장 어미 패턴(합쇼체·해요체·해라체)만 보는 결정적 분류이며
  존댓말 어휘, 높임 선어말어미, 문장 중간의 문체 변화는 구분하지 않는다.
- 서식 관찰은 Word·한글·PowerPoint에서 선택 텍스트 지문이 같고 굵기·크기·
  정렬 중 정확히 한 축만 분명하게 바뀐 경우만 지원한다. 한글은 선택 영역의
  `HCharShape`·`HParaShape` 값이 혼합·미정이면 관찰하지 않는다. 활성화된
  굵기·정렬·글자 크기 선호는 새 Word 또는 한글 업무 보고서 전체에는 미리보기와
  승인 뒤 자동 적용하고 COM read-back으로 검증한다. Word·한글·PowerPoint 일반
  편집에서는 사용자가 서식 항목만 말하고 값을 생략한 경우에만 승인된 기본값을
  미리보기로 제안한다. 새 PowerPoint의 제목·본문에도 승인된 굵기·크기·정렬을
  적용하지만 색상·테마·레이아웃까지 학습하지는 않는다. 한글의 `크게`·`작게`
  기본값은 현재 선택 글자 크기에서 2pt 증감하는 제한된 의미이며, 선택 영역의
  크기가 섞여 하나의 현재값으로 확인되지 않으면 적용하지 않는다.
- 학습된 표 서식은 현재 Word 보고서의 머리글·줄무늬 표현 수준이며 Office 고급
  테이블 테마를 그대로 복제하지 않는다.
- 반복 작업 순서는 학습 후보로 저장할 수 있지만 10단계의 분석→Word→PPT
  의존 순서는 안전을 위해 자동 재배열하지 않는다.
- 선호 파일 위치가 삭제되거나 접근할 수 없게 되면 해당 기본값은 적용하지 않고
  원본 Excel 폴더를 사용한다.
- 사용자 선호는 기본값만 제공하며 승인, 원본 지문 검증, 기존 파일 비덮어쓰기,
  VBA 고위험 재확인 정책을 줄이거나 우회하지 않는다.

- 명령·질문·편집 모드, 대화 기록, 확인 선택, 문서 선택, 설정 모달의 핵심 경로는
  키보드와 ARIA 접근성 트리로 검증했다. 다만 실제 NVDA 같은 화면 읽기 프로그램,
  음성 입력, 고령·비숙련 사용자의 장시간 관찰 시험은 아직 별도 수용 시험이
  필요하다.

- 10단계 보고서 산출물은 Word `.docx`, 한글 `.hwp` 또는 두 형식 동시 생성을
  지원한다. 형식을 말하지 않으면 Word를 사용한다. `HWPX`라는 명령 표현도 현재
  출력은 `.hwp`로 정규화한다. Word는 저장
  후 별도 인스턴스로 다시 열어 검증하지만, 한글은 저장한 같은 소유 문서에서
  내용·서식을 다시 읽고 파일 존재·지문을 검증한다.
- 10단계 Excel 분석은 표시 시트 최대 20개를 대상으로 한다. 시트별 최대
  50,000셀·30열, 전체 최대 100,000셀이며 값을 읽기 전에 범위 크기로 차단한다.
  숨김 시트는 제외하고 공통 모델에는 시트별 최대 100행·전체 최대 500행을 담는다.
  시트 간 관계 추론·조인·피벗 같은 복합 결합 분석은 후속 범위다.
- 10단계 차트는 제목과 형식 메타데이터만 추출하며 원본 차트 이미지나 Office
  디자인 템플릿을 복제하지 않는다.
- 검증된 10단계 복합 업무는 보고서 형식·PPT 장수·고정 단계 순서만 내용 없는
  스킬로 기억할 수 있다. `지난번처럼` 재생도 현재 Excel·현재 선호·새 출력
  경로를 다시 검사하고 매번 전체 승인을 요구한다. 임의 앱 조합, 사용자 정의
  단계, 원본/산출물 경로, 문서 내용은 복합 스킬로 저장하지 않으며 현재 명시한
  형식·장수만 기억한 구조를 덮어쓴다.
- Excel 편집 문맥의 `이거 보고서랑 발표자료 만들어줘`에서 `이거`는 선택 셀이나
  선택 범위가 아니라 현재 연결 Excel 전체를 뜻한다. 오동작을 줄이기 위해
  보고서·발표자료·생성 동사가 모두 있어야 하고 미리보기에 전체 분석임을
  표시한다. 선택 범위만으로 보고서·발표자료를 만드는 기능은 아직 지원하지 않는다.
- `방금 만든 보고서·발표자료 열어줘`는 현재 연결 Excel에서 가장 최근 완료된
  10단계 워크플로 산출물만 가리킨다. 경로·파일 지문·앱·전면 창을 모두
  재검증하며, 파일이 바뀌었거나 Word와 한글 보고서가 함께 있어 형식이
  모호하면 열지 않는다. 열기 명령만으로 산출물을 새 편집 문서로 연결하거나
  후속 내용을 수정하지는 않는다.
- `방금 만든 보고서를 편집 문서로 연결해줘`처럼 `편집`과 `연결`을 모두
  명시하면 검증된 최근 산출물을 새 편집 세션으로 전환한다. 전환에 실패하면
  기존 Excel 세션을 유지한다. 전환 자체는 내용을 수정하지 않으며 후속 편집
  전에 Word·한글은 원하는 범위나 커서를, PowerPoint는 대상 Shape를 사용자가
  선택해야 한다.
- 사용자가 이미 생성된 워크플로 산출물을 수정했다면 JARVIS는 그 파일을
  덮어쓰지 않는다. 수정 파일을 다른 위치로 옮긴 뒤 실패 단계를 재개해야 한다.

- 8단계에서 관찰됐던 PowerPoint 선택 Shape COM 프록시 오류는 안정 ID 기반
  재획득과 쓰기 후 재검증으로 보완했고, 보완 후 PowerPoint 500/500 실검을
  통과했습니다. 모든 Office 버전과 타사 추가 기능 조합까지 보증하는 결과는
  아닙니다.
- 9단계 Excel VBA 실제 쓰기·복원·매크로 실행 검증은 이 PC의 보안 센터에서
  `VBA 프로젝트 개체 모델에 안전하게 액세스`가 허용되지 않아 대기 중입니다.
  JARVIS는 이 보안 설정을 자동 변경하거나 우회하지 않습니다.
- 한글 8단계 probe는 파일 보안 모듈을 설치·변경하지 않고 JARVIS 소유 전용
  미저장 문서를 사용합니다. 따라서 해당 probe는 기존 파일 저장·재열기보다
  편집·복원·프로세스 안정성 검증에 초점을 둡니다.
- 현재 이 PC의 HWP COM은 소유 임시 문서의 `SaveAs`에서 응답하지 않는 경우가
  관찰됐고, 공식 Automation 보안 모듈 등록도 비어 있다. 모듈이 없거나 등록
  파일을 찾지 못하면 HWP를 시작하지 않고 `environment_error`로 즉시
  차단한다. 유효한 모듈을 등록한 뒤에도 저장 응답이 멈추면 별도 소유 프로세스와
  60초 watchdog으로 격리해 부분 파일을 제거하고 `timeout`으로 복귀한다.
  파일 접근 환경이 해결되기 전에는 새 `.hwp` 생성과 최근 한글 산출물 편집
  인계의 새 실검을 완료할 수 없다. JARVIS는 모듈 설치·레지스트리·보안 설정을
  자동 변경하거나 우회하지 않는다.
- VBA 코드 수정은 표준 모듈과 50,000자 이하 코드만 지원하며, 잠긴 프로젝트와
  Private/인수 있는 Sub 실행, 장시간 매크로 강제 중단은 지원하지 않습니다.
- VBA `Application.Run()`이 COM 오류 없이 반환된 것은 프로시저 호출 완료만
  뜻합니다. 매크로가 만든 셀·파일 등 업무 결과는 별도 후조건을 검증하지 않는 한
  확인 완료로 표시하지 않습니다.
- 한글 2024의 PDF Automation 저장은 간헐적 무응답 위험 때문에 자동
  실행을 차단합니다.
- Excel·한글 자동화는 Windows 데스크톱과 해당 프로그램이 실제로 실행
  중이어야 합니다.
- 여러 Office 인스턴스, 숨겨진 문서, 읽기 전용·보호 상태처럼 대상을
  안전하게 특정할 수 없으면 실행하지 않습니다.
- 안전한 단일 네이티브 학습 행동은 재실행할 수 있지만, 여러 앱 작업이
  섞인 복합 네이티브 학습 계획은 아직 자동 재실행하지 않습니다.
- 프로그램 실행 매크로는 일반 애플리케이션 실행만 지원합니다. 셸,
  스크립트, 시스템 관리 명령은 지원하지 않습니다.
- R6에서 외부 Gemini·OpenAI 실계정 테스트 3개는 실제 키 사용 승인이 없어
  건너뛰었습니다. 외부 AI 연결은 완전 검증으로 표시하지 않습니다.
- 실제 앱 검증은 이 PC에 설치된 Excel·한글 버전을 기준으로 하며 모든
  Office/HWP 버전 호환성을 보증하지 않습니다.
- 현재 실행 파일은 디지털 서명되지 않았습니다. 개인 로컬 사용만 허용하며
  외부 배포 전 Authenticode 서명과 타임스탬프가 필요합니다.
