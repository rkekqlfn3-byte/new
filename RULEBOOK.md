# JARVIS Rulebook

## 실행 파일 및 백업 정리 원칙

- 정리 완료 후에는 **현재 실행 가능한 프로그램 1개와 바로 이전 백업 1개만 보존**한다.
- 바로 이전 백업보다 오래된 EXE, ZIP, 릴리스 폴더와 중복 복사본은 삭제한다.
- 선택한 실행 방식(EXE 배포본 또는 소스 실행)에 필요하지 않은 파일은 모두 삭제 대상이다.
- 테스트 코드와 테스트 자료(`test_*.py`, 테스트 문서, fixtures), 검증 스크립트와 생성 보고서, 빌드 중간 산출물, 캐시, 임시 파일, 오래된 로그는 프로그램 실행에 필요하지 않으면 삭제한다.
- 개발 도구와 가상환경은 현재 실행 방식에 필요하지 않으면 삭제한다. 단, 소스 실행을 유지하는 경우에는 실행에 필요한 소스, GUI, 기본 데이터, 의존성 파일과 정상 가상환경을 보존한다.
- 사용자 데이터, API 설정, 대화 세션, 사용자 사전, 학습 매크로와 사용자 문서는 정리 대상으로 취급하지 않는다.

## 삭제 전 필수 확인

1. 현재 실행본의 절대 경로와 실행 방식을 확정한다.
2. 현재 실행본의 시작·종료 스모크 테스트와 SHA-256 검증을 통과시킨다.
3. 보존할 직전 백업 1개의 절대 경로와 SHA-256을 확인한다.
4. 삭제 후보 목록을 먼저 출력하고, 대상이 프로젝트 또는 지정된 산출물 폴더 내부인지 확인한다.
5. 현재 실행본, 직전 백업, 사용자 데이터가 삭제 목록에 없는지 재확인한 뒤 삭제한다.
6. 정리 후 프로그램 재실행과 사용자 데이터 보존을 다시 검증한다.

이 규칙에 따른 정리는 기능 수정·리팩터링과 분리해서 수행한다.
