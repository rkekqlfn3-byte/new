# JARVIS Rulebook

## 1. 폴더 역할

### 개발 저장소

현재 Git 작업 폴더는 기능 개발과 재현 검증을 위한 저장소다. 다음 항목은
프로그램 실행 파일이 아니더라도 개발 저장소의 필수 구성으로 보존한다.

- `engine`, `gui`, `default_data`, 진입 스크립트
- `tests`, `verification`
- `Jarvis.spec`, `build_dist.bat`, 요구사항 잠금, `pyproject.toml`
- README, 변경 기록, 제한사항, 감사·계획 문서, `.git`

테스트·검증·빌드 파일은 실행 배포물에 포함하지 않는다.

### 실행 폴더

실행 폴더는
`C:\Users\PC04\OneDrive\Desktop\JARVIS_RUNTIME`으로 고정한다.

```text
JARVIS_RUNTIME/
├─ current/Jarvis/       # 현재 정상 실행 가능한 풀린 배포본 1개
└─ previous/Jarvis.zip   # 바로 이전 정상 실행본 백업 1개
```

정리 완료 후 실행 폴더에는 위 현재본 1개와 직전 백업 1개만 보존한다. 그보다
오래된 EXE·ZIP·릴리스 폴더, 중복 복사본, 소스, Git, 테스트, 검증 도구,
빌드 도구, 보고서, 캐시와 임시 파일은 삭제한다.

## 2. 개발 저장소 정리 원칙

- `.venv`, `build`, `dist`, `releases`, `__pycache__`, 테스트 결과, 생성 보고서,
  임시 파일과 캐시는 작업이 끝나면 삭제한다.
- 소스·테스트·검증·빌드 설정·개발 문서는 삭제하지 않는다.
- 후보 빌드는 현재 실행본을 덮어쓰지 않고 별도 후보 경로에서 검증한다.
- Git이 추적하는 파일을 “실행에 필요 없다”는 이유만으로 삭제하지 않는다.

## 3. 사용자 데이터 보호

`%LOCALAPPDATA%\Jarvis\data`의 사용자 데이터, API 설정, 대화 세션, 사용자
사전, 학습 매크로와 사용자 문서는 정리·빌드·배포 대상으로 취급하지 않는다.
검증이 요구하는 경우에도 사전·사후 해시만 비교하고 초기화하거나 덮어쓰지
않는다.

## 4. 삭제 또는 릴리스 교체 전 필수 확인

1. 삭제 대상과 보존 대상의 절대 경로를 확정한다.
2. 현재 실행본과 직전 백업의 SHA-256을 기록한다.
3. 현재 실행본의 시작·종료와 사용자 데이터 무변경을 확인한다.
4. 삭제 후보 목록을 먼저 출력하고 대상이 지정 폴더 안에 있는지 확인한다.
5. 현재 실행본, 직전 백업, 사용자 데이터, Git 추적 개발 파일이 삭제 목록에
   없는지 재확인한다.
6. 새 후보의 전체 테스트와 실제 앱 검증이 모두 통과한 경우에만 기존
   current를 previous로 승격하고 후보를 current로 교체한다.
7. 교체 후 새 current 재실행, build identity, 해시, 사용자 데이터 보존을
   다시 검증한다.

이 규칙에 따른 정리는 기능 수정·리팩터링과 별도 커밋으로 수행한다.
