# JARVIS V1 통합 감사 보고서

- 감사 일자: 2026-07-15 (KST)
- 감사 대상: 현재 소스 트리, Git 이력, GUI 자산, 기본 데이터, 현재 EXE 배포본, 직전 백업
- 현재 판정: **조건부 통과**
- 현재 실행본: `dist/Jarvis/Jarvis.exe`
- 실행본 버전: `1.1.0-preview.2`, 데이터 스키마 `4`

## 1. 최종 결론

현재 JARVIS 실행본은 정상 기동하며, 감사 중 사용자 데이터 변경이나 비밀정보 혼입, 즉시 실행을 중단해야 할 P0 결함은 확인되지 않았다. 직전 전체 회귀 결과도 실패 0건이고 실제 Excel·HWP 자동화 검증을 통과했으므로, **신뢰할 수 있는 로컬 문서만 다루는 개인용 실행본으로는 계속 사용할 수 있다.**

다만 완전 통과로 판정할 수는 없다. PDF 처리에 실제 사용되는 `PyPDF2 3.0.1`에서 중간 등급 서비스 거부 취약점 1건이 확인됐고, 테스트·빌드 도구를 정리하면서 현재 체크아웃만으로 전체 검증과 EXE 재빌드를 반복할 수 없게 됐다. README·CHANGELOG·제한사항 문서도 실제 완료 상태 및 현재 파일 구성과 맞지 않는다.

따라서 다음 릴리스나 Word·PowerPoint 개발을 시작하기 전에 아래 P1 3건을 먼저 처리하는 것이 안전하다. 또한 통합 계획서의 우선 작업은 상당 부분 구현됐지만 **계획서의 최종 완료 기준은 아직 충족되지 않았다.**

| 등급 | 건수 | 의미 |
| --- | ---: | --- |
| P0 | 0 | 즉시 사용 중단이 필요한 치명적 결함 |
| P1 | 3 | 다음 릴리스·기능 개발 전에 반드시 조치 |
| P2 | 3 | 현재 동작은 가능하지만 유지보수 단계에서 조치 |
| P3/참고 | 2 | 배포 품질과 추적성 개선 항목 |

## 2. 감사 기준선

### 현재 실행본

- 절대 경로: `C:\Users\PC04\OneDrive\Desktop\jarvis_work (1)\dist\Jarvis\Jarvis.exe`
- SHA-256: `B237B5678C147988431CE93EAE66904A5A12474985DA6154B1C2D57C42FE8455`
- 파일 크기: 8,256,572바이트
- 전체 배포 폴더: 120개 파일, 34,721,530바이트
- 빌드 소스 커밋: `a096e9303e6f339e1c9c57472183f5c6d901af39`
- 빌드 시각: `2026-07-15T02:55:12+00:00`
- `win32ui.pyd` 포함, 불필요한 `win32trace.pyd` 미포함

### 직전 백업

- 경로: `releases/JARVIS_Command_Engine_V1/Jarvis.zip`
- SHA-256: `D4F485386AC47AB0A9C2B4697462B5BB5FDA7D6121AE04E4C50AE1D6B4599116`
- 파일 크기: 16,808,381바이트

### 현재 Git 상태

- 감사 시작 HEAD: `9f3d734d778595b946e4c50fe547fdfbe38d4f1a`
- 제목: `RULEBOOK 테스트 및 빌드 잔여물 최종 정리`
- 실행본 빌드 커밋과 현재 HEAD 사이의 차이는 테스트·검증·빌드·문서 파일 정리뿐이다.
- `jarvis_app.py`, `engine`, `gui`, `default_data`, 런타임 의존성 등 실행 코드에는 차이가 없다.

## 3. 통과한 항목

### 실행 및 데이터 안전성

- 현재 EXE를 새로 실행해 5초 뒤에도 프로세스가 정상 생존함을 확인했다.
- 감사 종료 시 감사에서 시작한 프로세스만 종료했다.
- 실행 전후 `%LOCALAPPDATA%\Jarvis\data`의 33개 파일을 경로·크기·SHA-256으로 비교했으며 바이트 단위로 동일했다.
- 기본 데이터 JSON 3개는 모두 정상 파싱됐고 스키마 버전은 `4`였다.
- 기본 데이터에는 API 키, 학습 매크로, 사용자 명사 등 개인 데이터가 들어 있지 않았다.

### 회귀 및 실제 앱 검증 이력

정리 직전 동일 실행 코드에서 다음 결과를 확인했다. 이후 커밋은 비실행 파일 삭제만 포함한다.

- 전체 자동 테스트: 420개 중 417개 통과, 외부 AI 선택 테스트 3개 건너뜀, 실패 0개
- 통합 테스트: 229/229 통과
- 실제 Excel 시나리오: 20/20 통과
- 실제 HWP 시나리오: 8/8 통과
- EXE 워커 격리, 덮어쓰기 정책, 재시도, 잠금, 취소 복구 검증 통과
- EXE Excel COM 저장·재열기 및 수식·표식 왕복 검증 통과
- 기존 사용자 데이터와 신규 데이터 환경의 EXE 기동 검증 통과

이 과정에서 발견됐던 파이프라인 상태 참조 오류, 패키징된 `win32ui` 누락, 동결 워커의 사용자 데이터 조기 초기화 문제는 모두 수정됐고 회귀 검증을 통과했다.

### 정적 코드 및 GUI 검사

- Python 소스: 92개 파일, 21,577줄, 구문 오류 0건
- 로컬 `engine.*` import 누락 0건
- `eval()`·`exec()` 호출 0건
- `subprocess`의 `shell=True` 호출 0건
- TODO·FIXME·HACK 표식 0건
- JavaScript: 18개 파일, 구문 오류 0건
- CSS: 9개 파일, 중괄호 불일치 0건
- 인라인 이벤트 핸들러 0건
- 외부 원격 스크립트·스타일 참조 0건
- `/eel.js`는 Eel이 실행 중 동적으로 제공하는 정상 경로다.

### 비밀정보 및 배포물 검사

- 소스·기본 데이터에서 일반적인 API 키·토큰 패턴이 확인되지 않았다.
- 현재 배포본과 직전 ZIP을 모두 검사했으며, 현재 로컬 API 키의 지문과 일치하는 내용이 없었다.
- 세션, 백업 데이터, 진단 로그, 시작 오류 로그 등 개인 파일이 배포물에 포함되지 않았다.
- 배포물 안에 사설 JSON 데이터가 포함되지 않았다.

## 4. 미해결 감사 지적

### P1-SEC-001 — 실제 사용 중인 PDF 라이브러리에 알려진 서비스 거부 취약점

`requirements-lock.txt`는 `PyPDF2==3.0.1`을 고정하고 있고, `engine/document_reader.py`에서 PDF 페이지의 `extract_text()`를 실제 호출한다. `engine/app_actions/hwp_adapter.py`도 PDF 페이지 수 검증에 같은 라이브러리를 사용한다.

2026-07-15에 `pip-audit`로 잠금 파일의 19개 의존성을 조회한 결과, `PyPDF2 3.0.1`에서 `PYSEC-2026-1835` 1건이 확인됐다. OSV 기준 CVSS 3.1 점수는 6.2(중간)이며, 특수하게 조작된 PDF의 콘텐츠 스트림을 해석할 때 무한 루프가 발생해 프로세스 한 코어를 100% 점유할 수 있다.

- 영향: 신뢰하지 않는 PDF를 읽을 때 JARVIS가 응답 불능 상태가 될 수 있음
- 현재 임시 방어: 출처가 확실한 PDF만 열기
- 권고 조치: 유지가 종료된 `PyPDF2` import를 지원 중인 `pypdf`로 교체하고, 이 취약점의 최소 수정선인 `3.9.0`보다 최신인 감사된 버전을 고정한 뒤 PDF·HWP·배치 회귀 테스트 및 EXE 재빌드 수행
- 근거: [OSV PYSEC-2026-1835](https://osv.dev/vulnerability/PYSEC-2026-1835), [PyPDF2 프로젝트의 pypdf 전환 안내](https://pypi.org/project/PyPDF2/)

### P1-OPS-002 — 실행 폴더와 개발 저장소의 역할 충돌

RULEBOOK을 EXE 실행 폴더에 엄격히 적용하면 소스, `.git`, 요구사항 파일도 실행에는 필요하지 않으므로 삭제 대상이다. 반대로 Word·PowerPoint 기능을 계속 개발하려면 소스뿐 아니라 테스트, 검증 스크립트, PyInstaller 설정과 빌드 잠금 파일이 필요하다.

현재 상태는 소스와 Git은 남겼지만 `tests`, `verification`, `Jarvis.spec`, `build_dist.bat`, `requirements-build.txt`, `pyproject.toml`은 삭제된 혼합 상태다. 실행에는 문제가 없고 Git의 `a096e93`에서 복구할 수 있지만, 현재 체크아웃만으로 전체 회귀나 동일 EXE 재빌드를 즉시 반복할 수 없다.

- 영향: 다음 기능 개발 중 회귀 검증 및 재현 빌드가 중단될 수 있음
- 권고 조치: 운영 폴더와 개발 저장소를 물리적으로 분리
  - 운영 폴더: 현재 배포본 1개와 직전 백업 1개만 보존
  - 개발 저장소: 소스, Git, 테스트, 검증, 빌드 설정을 보존하되 운영 배포물에는 포함하지 않음
- RULEBOOK에도 “개발 저장소는 실행 폴더 정리 규칙의 대상이 아니며, 배포 패키지에는 포함하지 않는다”는 예외를 명시할 필요가 있음

### P1-DOC-003 — 문서가 실제 상태 및 현재 파일 구성과 불일치

`README.md`, `CHANGELOG.md`, `KNOWN_LIMITATIONS.md`에 다음과 같은 오래된 정보가 남아 있다.

- 삭제된 `requirements-build.txt`, `build_dist.bat`, `tests/test_runner.py`, `docs/audits/archive`를 사용하거나 참조하도록 안내
- P0 변경분의 전체 회귀 및 실제 Office 검증이 아직 미완료라고 기재
- 실제 최종 결과 420개/417개 통과/3개 건너뜀 대신 이전 기준점 418개/415개를 기재
- 이미 구현된 일부 Excel·HWP 학습 실행 범위를 미구현으로 설명

권고 조치는 개발·운영 폴더 분리 방침을 먼저 확정한 뒤 세 문서를 한 번에 현재 기준으로 갱신하는 것이다.

### P2-CODE-004 — 핵심 모듈과 함수의 복잡도가 여전히 높음

P1 정리로 구조가 개선됐지만 다음 파일은 계속 변경 충돌과 회귀 위험이 큰 편이다.

- `engine/app_actions/excel_adapter.py`: 1,820줄
- `engine/parser.py`: 1,615줄
- `engine/action_executor.py`: 1,063줄
- `engine/app_actions/hwp_adapter.py`: 873줄
- 120줄 이상 함수: 18개
- 최대 함수: `batch_executor.execute` 399줄, `command_pipeline.execute` 335줄

현재 동작 결함으로 판정하지는 않는다. Word·PowerPoint를 붙이기 전에 앱별 명령 해석, 실행 정책, 검증 로직을 더 작은 단위로 분리하고 단위 테스트 경계를 함께 복원하는 것이 좋다.

### P2-CODE-005 — 종료 명령이 셸 실행 결과를 확인하지 않고 성공을 반환

`engine/builtins.py`의 시스템 종료 및 종료 취소는 고정 문자열을 `os.system()`으로 실행한 뒤 반환 코드를 확인하지 않고 성공 결과를 만든다. 사용자 입력을 명령 문자열에 결합하지 않으므로 현재 코드에서 명령 주입 경로는 확인되지 않았지만, 실제 명령이 실패해도 UI가 성공으로 표시할 수 있다.

권고 조치는 `subprocess.run(["shutdown", ...], shell=False)` 형태로 교체하고 반환 코드와 오류 메시지를 검사해 실행 결과를 확정하는 것이다. 같은 파일의 창 없는 프로세스 강제 종료 구간에 있는 `except: pass`도 구체적 예외 및 로그로 바꾸는 편이 좋다.

### P2-SEC-006 — GUI의 동적 HTML 삽입 경계가 완전히 정리되지 않음

채팅 Markdown 경로는 허용 목록 정화기를 거치고, 사전·학습 화면의 주요 문자열도 대부분 `escapeDictionaryHtml()` 또는 `textContent`로 처리한다. 그러나 `gui/js/dictionaries/action_dictionary.js`의 오류 화면은 `${e.message}`를 그대로 `innerHTML`에 삽입한다. 학습 카드에도 `record.step_count` 등 숫자라고 가정한 일부 값이 HTML 속성에 직접 들어간다.

현재는 로컬 데이터와 로컬 Eel UI라는 조건 때문에 위험도가 제한적이지만, 변조된 저장 데이터나 예외 메시지가 들어오면 저장형 또는 반사형 DOM 삽입 경계가 될 수 있다. Eel에 노출된 백엔드 API가 많으므로 외부 배포 전에는 남아 있는 동적 템플릿을 모두 점검해야 한다.

- 권고 조치: 오류 메시지는 `textContent`로 출력
- 숫자 필드는 `Number()` 변환 및 유효 범위 검증 후 사용
- 가능한 화면은 DOM 노드 생성과 `replaceChildren()`로 전환
- 테스트 도구를 복원한 뒤 사전·매크로·학습·후보 화면에 악성 문자열 회귀 테스트 추가

## 5. P3 및 참고 사항

### 빌드 추적성

현재 EXE의 내장 빌드 커밋은 `a096e93`이고 현재 HEAD는 정리 커밋 `9f3d734`다. 런타임 소스 차이는 없으므로 실행상 문제는 아니지만, 정식 릴리스에는 “빌드 커밋 + 정리 커밋”의 관계를 릴리스 노트나 태그로 명시하는 것이 좋다.

### 디지털 서명

현재 `Jarvis.exe`의 Authenticode 상태는 `NotSigned`다. 개인 로컬 사용에는 필수 사항이 아니지만 외부 배포를 시작하면 Windows 신뢰 경고와 배포물 위변조 확인을 위해 코드 서명 및 타임스탬프 적용을 검토해야 한다.

## 6. 통합 계획서 준수 상태

계획서의 P0·P1·P2는 순차 단계가 아니라 우선순위 묶음이다. 원문의 각 항목과 현재 상태를 다시 대조한 결과는 다음과 같다.

### P0 — 8월 최종 검증 전 필수 항목

| 계획 항목 | 판정 | 감사 근거 |
| --- | --- | --- |
| Git 커밋·기준 태그 | 통과 | `p0-baseline-20260715` 태그와 후속 커밋 존재 |
| API 키 GUI 비노출 | 통과 | 공개 설정 응답과 GUI가 실제 키를 보관하지 않도록 분리 |
| 사용자 매크로 `shell=True` 제한 | 통과 | `shell=True` 0건, 동적 매크로 실행 정책·격리 검증 통과 |
| 예외·로그 처리 | 부분 | 중앙 로그는 도입했으나 bare `except` 1건과 스캐너 등의 `print()`가 남음 |
| 깨끗한 환경의 의존성 잠금 | 부분 | 런타임 잠금은 존재하지만 현재 빌드 잠금·빌드 도구는 삭제됨 |
| 소스·EXE 버전·커밋 일치 | 부분 | 런타임 소스는 동일하지만 EXE는 `a096e93`, HEAD는 정리 커밋 `9f3d734` |
| 낡은 감사·테스트 수치 정리 | 미통과 | README·CHANGELOG·KNOWN_LIMITATIONS가 오래된 상태 |
| 전체 회귀·실제 Excel·HWP | 통과 | 420개 회귀, Excel 20/20, HWP 8/8 |

P0는 **4개 통과, 3개 부분 통과, 1개 미통과**이므로 완료 판정할 수 없다.

### P1 — 기능 확장 전 권장 항목

| 계획 항목 | 판정 | 감사 근거 |
| --- | --- | --- |
| `parser.py` 단계적 분리 | 부분 | 2,832줄에서 1,615줄로 감소, dispatcher·pipeline 분리. 목표 700~1,000줄은 미달 |
| AI 실행 처리 분리 | 통과 | validator·preflight·executor·descriptor 구조 존재 |
| LLM 제공자 중복 제거 | 통과 | OpenAI·Gemini·Ollama 제공자 모듈 분리 |
| GUI JavaScript·CSS 분리 | 통과 | JS 18개, CSS 9개로 분리, 인라인 이벤트 0건 |
| 테스트 실행 체계 분리 | 부분 | 정리 전 `unit/integration/windows/live` 구조를 완성했지만 현재 체크아웃에서는 삭제됨 |
| Excel·HWP 네이티브 학습 재실행 | 통과 | 안전한 단일 `app_command` 재실행 계약 구현, 복합 네이티브는 계획대로 보류 |
| 전역 싱글턴 초기화 개선 | 통과 | 런타임 경로 초기화 후 코어·API를 구성하고 동결 워커를 선분기 |

P1은 **5개 통과, 2개 부분 통과**다.

### P2 — 선택적 경량화

- 불필요 모듈 제외와 배포 메타데이터 축소를 보수적으로 적용했다.
- 실제 Pywinauto가 요구하는 `pythonwin/win32ui`는 유지하고 `win32trace`만 제외했다.
- UPX는 잠금된 빌드 도구가 아니므로 우발적인 빌드 차이를 막기 위해 명시적으로 비활성화했다.
- 현재 배포 폴더는 34,721,530바이트로 목표 범위에 가깝고, 기능 안정성을 희생할 추가 경량화는 하지 않았다.

### 최종 완료 기준 판정

| 영역 | 판정 | 남은 조건 |
| --- | --- | --- |
| 저장소 | 부분 | 운영 폴더·개발 저장소 분리, 소스 패키지 재현 절차 |
| 코드 | 미통과 | `parser.py` 1,615줄, AI batch `execute` 399줄, bare `except` 1건, 동적 HTML 경계 잔존 |
| 빌드 | 부분 | 현재 체크아웃에 빌드 도구 없음, EXE 커밋과 HEAD가 엄격히 일치하지 않음 |
| 기능 | 통과 | Excel·HWP, 확인·취소·busy, 학습·재사용, 사용자 데이터 보존 검증 |
| 문서 | 미통과 | README·CHANGELOG·KNOWN_LIMITATIONS 최신화 필요 |

결론적으로 **실행 가능한 Excel·HWP 범위는 안정화됐지만 계획서 전체는 끝나지 않았다.** Word·PowerPoint 네이티브 편집도 아직 구현되지 않았다.

## 7. 권장 조치 순서

1. `PyPDF2`를 지원 중인 `pypdf`로 전환하고 잠금 파일을 재생성한다.
2. 운영 폴더와 개발 저장소를 분리하고, 개발 저장소에 테스트·검증·빌드 도구를 Git 이력에서 복원한다.
3. PDF/HWP/배치 테스트, 전체 420개 회귀, 실제 Office 검증, EXE 보안 검사를 다시 실행한다.
4. 새 EXE를 빌드하고 빌드 커밋·SHA-256·백업 교체 내역을 기록한다.
5. README·CHANGELOG·KNOWN_LIMITATIONS·RULEBOOK을 실제 구조와 결과에 맞게 갱신한다.
6. 남은 동적 `innerHTML`, bare `except`, 무조건 성공 반환 경로를 정리한다.
7. Word·PowerPoint 구현 전에 대형 모듈과 함수의 분리 작업을 진행한다.

## 8. 감사 범위의 한계

- 외부 Gemini·OpenAI 실계정 호출 테스트 3개는 활성화하지 않았다.
- 이번 감사는 자동 정적 검사, 기존 회귀 결과 검증, 로컬 실행 스모크, 의존성 취약점 조회 중심이다. 전문 침투 테스트나 악성 PDF 퍼징은 수행하지 않았다.
- 실제 Excel·HWP 검증은 현재 PC의 설치 버전 기준이며 모든 Office/HWP 버전 호환성을 보증하지 않는다.
- 알려진 취약점 데이터베이스 결과는 2026-07-15 조회 시점 기준이다.

## 9. 승인 판정

- 현재 개인용 실행: **조건부 승인** — 신뢰할 수 있는 PDF만 사용
- 새 배포본 공개 또는 타인 배포: **보류** — P1-SEC-001 우선 수정
- Word·PowerPoint 개발 시작: **보류 권고** — P1-OPS-002와 P1-DOC-003을 먼저 정리
- 모든 P1 조치 및 재회귀 통과 후: **완전 통과 재심사 가능**
